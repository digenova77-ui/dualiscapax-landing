# Apex mobile spine — 2026-09-05

Drop these three files onto the Cloudflare origin that currently serves dualiscapax.ai (not GitHub `main` / story.html).

- `index.html`
- `styles.css`
- `app.js`

Cache bump: `?v=136`

## What changed
- Geodesic logo centered above the wordmark on every view
- Title letter-spaced wider; green subtitle flush to the same width (`UNITY` · `PLAYGROUND` · `ARCHITECTURE`)
- Four pillars stacked vertically (no 2×2)
- `#report` and `$0 Onboarding` are a bind page: banner, logo, four fields, one agreement line
- Shared realm CSS: quark spray + spinning geodesic conduit on `#gl-canvas` for the whole structure

## Do not
Replace GitHub `main` `index.html` (that file still redirects to `story.html`).
This pack is the compact playground already live on the apex host.
