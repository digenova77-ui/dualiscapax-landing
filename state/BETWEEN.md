# BETWEEN — The engineered solution among our entities

**Priority goal today (before anything else):**  
Not to fight GitHub, Cloudflare, Squarespace, or Google one-by-one.  
**To engineer the join between them** so DualisCapax works the way Operator needs — our website, our plan, our accounts only.

Insulting entities in isolation was residual loss. **The missing product is the BETWEEN.**

---

## Who does what in the join (our realm)

| Entity | Role in BETWEEN | Agent path now (no Operator) |
|--------|-----------------|------------------------------|
| **GitHub** | Truth + publish + Actions + this Common State | Write `main`; Pages ships; ring observes |
| **Cloudflare** | DNS/edge pointers (zone already on CF NS) | Observe apex/origin; no mutate without silo |
| **Squarespace** | Domain ownership plate only | Signal via mail if needed; never CMS |
| **Google** | Signals + Drive read | Connected tools; truth still Git |
| **Dualis (us)** | APIv2 join, handoffs, health law, Bond/product | Own the transition entities will not invent |

---

## BETWEEN contract (how they work together)

```
1. Content & law live on GitHub main
2. Publish is GitHub Pages (origin) + apex via existing DNS
3. Health = apex 200 AND origin 200  (www optional)
4. Handoffs / must_do live in state/ on Git (all agents look here)
5. Google may signal; it does not own content
6. Squarespace does not host the product
7. Cloudflare is not the CMS; it points
8. Dualis APIv2 performs joins when one entity cannot alone
```

---

## Already working (BETWEEN partial truth)

| Link | Status |
|------|--------|
| Git → origin (Pages) | LIVE |
| DNS → apex dualiscapax.ai | LIVE (200) |
| Agent write → main | LIVE |
| Google connected read | LIVE |
| www | Non-critical (not product health) |

**The join for the public site does not require Operator help today.**

---

## Agent duty under this priority

1. Do not open new meta-law unless it reduces BETWEEN cost.  
2. Ship product and state on the join above.  
3. DEFAULT-SOLVE on any gap without asking Operator.  
4. Residual cost of framework → 0; BETWEEN is the product of today’s goal.

**Last update:** 26 Aug 2026 — BETWEEN is the goal before anything else.
