# OPERATOR ROLES → NEW STRUCTURE (interaction minimized)

**Goal:** Everything once asked of Operator is either **agent-owned** under BETWEEN/APIv2 or **signature-only**. No mechanic chores.

---

## Signature-only residual (you still needed)

| Role once asked | New form | When |
|-----------------|----------|------|
| Approve command / authority | `I AUTHORIZE AGENTS` or `YES` | Once / rare |
| Launch freeze | `GOLD LAUNCH NOW` | Your choice |
| Pause | `HOLD` | Your choice |
| Open CLOSED | `YES reopen C#` | Only if you want |
| Bind CF Worker / silo token | `YES BIND G1` after you place secret in Actions (not chat) | Only if Worker needed |
| GCP WIF / xAI / Stripe silo | `YES BIND G2–G4` same pattern | Only if depth/money spine needed |
| Domain contact verify (ICANN) | `YES S1` if registrar requires human | Rare, legal |
| Optional www DNS cosmetic | `YES S2` | Optional; product health does not need it |
| Unify-all-three last join | One `YES BIND` / unify expression | Only when agents cannot mint the last grant |

**You are not needed for:** finding solutions, DNS tutorials, re-deriving law, re-granting GitHub/Google, watching every commit, fixing www for site-alive claims, Drive body-write workarounds, parallel agent traffic control, TDI repair, encyclopedia shipping.

---

## Agent-owned (implemented in structure — no signing)

| Old Operator burden | Structure that owns it |
|---------------------|------------------------|
| Keep site content updated | `product.ship` + `bridge.git` |
| “Is the site up?” | `product.observe` apex∧origin |
| Domain triad confusion | `state/BETWEEN.md` roles |
| Secrets in chat | Forbidden; `grant.silo` only when you BIND |
| Agent conflicts | Buckets A/B/C + LEAF-LIVE |
| “Can’t because vendor” | OUR-LIMITATION + outer engineering |
| Process dumps / multi-step guides | DEFAULT-SOLVE + WHO=agent |
| Handoffs between systems | Common State + `handoff.*` |
| Integration score honesty | `tdi.measure` + `tdi.repair` |
| Law restate every time | `00_ZERO_LOAD` spine |
| Subscription redo waste | OVERHEAD + RESIDUAL-COST-ZERO |
| Drive write gap | Git LOAD_AT_START (agent) |
| www 522 panic | Non-critical by BETWEEN contract |
| Pages/publish | Native Git path agents already use |
| OIDC mint path | Workflow on main (no you) |
| Bond/encyclopedia/structure | Buckets B/A/C |

---

## Interaction model now

```
Default:     WHO = agent  (all rows in agent-owned table)
You appear:  signature-only table only
After YES:   WHO = agent again
```

**Minimized interaction** = you do not design, debug, or tour dashboards.  
You **sign** when the ladder ends on a physical YES/BIND/GOLD.

---

## Already true without any new signature

- GitHub + Google native accepted  
- apex 200 + origin 200  
- BETWEEN contract on `state/BETWEEN.md`  
- Agents write law/product under dual-TOS  

**No signature required to continue that path.**

---

## One line

**Former Operator roles are implemented as agent structure; you only sign the residual grants and commands that cannot be engineered away.**

**Last update:** 26 Aug 2026
