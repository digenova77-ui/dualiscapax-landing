/**
 * DualisCapax payment processor links
 * Paste full https Payment Link / Checkout URLs from your processor (e.g. Stripe).
 * Empty string = button stays disabled on participate.html
 *
 * Lanes:
 *   cost_reduction — waterfall #1 (residual plane cost)
 *   capacity       — enterprise prepaid
 *   fuel           — escalate-only depth
 *   ubi            — waterfall #2 (after cost)
 */
window.DC_PAYMENTS = {
  cost_reduction: "",
  capacity: "",
  fuel: "",
  ubi: ""
};
