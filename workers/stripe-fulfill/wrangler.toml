name = "dualiscapax-stripe-fulfill"
main = "worker.js"
compatibility_date = "2026-08-30"

# Secrets (wrangler secret put …):
#   STRIPE_WEBHOOK_SECRET   whsec_… from Dashboard endpoint
#   STRIPE_SECRET_KEY       rk_… or sk_… (server only; never commit)
# Optional:
#   FULFILL_LOG_KV          bind a KV namespace for idempotent session ids
