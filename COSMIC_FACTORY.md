# Cosmic Factory Architecture — DualisCapax mapping

Extracted logic, not a second homepage.

## Primitive

```
S_t  --T,C-->  S_{t+1}
```

- **S** state: public face (root `index.html`), Iris quaternion `q`, DSAP wake/wave, story beat, deploy SHA
- **T** transformation: gesture wake, sphere draw, story expansion, pack-self-deploy, pages-direct-upload
- **C** constraints: `NO_FORCE`, `HOST_SAFE`, `CLEANUP_FIRST`, `TRUTH_OR_NOTHING`, single public face, checkout closed

## Recursion (locked)

Reality → Physical transformation → Observable structure → Logical abstraction → Engineering → New physical transformation.

On this repo that is:

1. Live visitor gesture (physical)
2. DSAP unlock + quaternion spin (observable)
3. CosmicFactory.tick receipt (logical)
4. Pack + Cloudflare/GH Pages publish (engineering)
5. Next visitor sees the new state (return to reality)

## DCLM Matrix Square (this pass)

| Cell | Claim | Verdict |
|------|--------|---------|
| Interface × Engine | Storyboard skin must load sphere + DSAP, not SpeechSynthesis | FAIL until root index scripts land |
| Repo × Live | `cf-pages` Iris stack exists; apex DNS is GH Pages serving root index | FAIL until root splice + publish |
| Iris × DSAP | Sphere energy from `DSAP.wave()`; wake on first gesture only | FAIL on live; PASS in `cf-pages/js` |
| Factory × Publish | mill/audit succeed; `pages-direct-upload` idle; GH Pages republishes harvests | FAIL until confirm=DEPLOY |

## Twain Square (triple pass on remaining work)

1. **Clean language** — one story: we stop the leak. Iris is a sphere, not a person.
2. **Clean logic** — DualisAV must not steal `#iris-sphere`. Wake with `null` canvas.
3. **Clean handoff** — pack `cf-pages` *and* elevate root index because DOMAIN triad origin is GitHub Pages.

## Rooms that actually exist (do not link ghosts)

Live 200: `/`, `/ai/`, `/research/`, `/corporate`
cf-pages 200 in tree: `iris.html`, `residual.html`, `ai/iris.html`
Live 404: `/onboarding`

Story links must point at the 200 set.

## Law floor

NO_FORCE — no autoplay. First pointer/key wakes audio.
HOST_SAFE — suspend AudioContext on hide.
CLEANUP_FIRST — DSAP.sleep / DualisAV refuse stolen canvas.
TRUTH_OR_NOTHING — do not claim Iris is live if the live HTML is a TTS button.
