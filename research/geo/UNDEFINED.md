# UNDEFINED — empiricist null is not anti-logic
2026-08-27 02:14 EDT
Owner: define Pure Index 0 (anti-logic) as what empiricist data would call undefined.

## Keep the cousin
When a formula has **no value in the domain**, empiricists say undefined.
When an organ **cannot close a loop that holds**, Pure Index says 0.
Those can point at the same empty chair: nothing here to invert.

## Do not smash
| Dualis state | Empiricist cousin | Pure Index |
|--------------|-------------------|------------|
| **HOLE** | measure defined, page silent | not 0 — hook exists |
| **N/A** | wrong species for this CSD (no subway in Belleville) | not 0 — city still holds |
| **ABSENT** | overlay class vs another city | not 0 |
| **unsearched** | we have not looked | not 0 |
| **cache miss** | live SRC not pulled | not 0 |
| **no organ** | undefined in the domain | **0** — anti-logic |

A city that prints a levy is not undefined. A missing WPCP rating is HOLE. Treating HOLE as L=0 would stamp operating towns as anti-logic.

## Agent rule
Say undefined only for *no domain*. Say HOLE when the hook is named and the page is quiet. Reserve 0 for cannot-hold.
