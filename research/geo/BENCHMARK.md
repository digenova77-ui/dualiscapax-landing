# BENCHMARK — ceiling and N/A
2026-08-27 00:36 EDT

Owner lock: no Canadian body more complex than Metro Toronto / the GTA cluster will be commanded. That is the ceiling for this project.

Two plates, not one:
- **Atom plate** = Belleville. Start. Every other Ontario CSD is read against it.
- **Ceiling plate** = City of Toronto CSD (`research/geo/toronto/SEAL.md`). Hardest single municipality on the 444.
- **GTA** is not a municipality. It is many organisms (Toronto + Mississauga + Brampton + Markham + Vaughan + …). Indexing the GTA is a cluster pass, not a smash into one row. Pearson lives in Mississauga. Do not swallow.

## Marker states (use these words only)
`SEALED` — public page printed a number or name.
`HOLE` — this organism *has* the organ; we have not read the page yet (TCHC units, ice $).
`N/A` — this organism *does not have* the organ. Belleville `TRANSIT.RAIL` subway = N/A. Belleville `SPORT.PRO` NHL = N/A. Belleville `WTP.MULTI` = N/A (one plant is not multi).
`RESIDUAL` — invented, smashed, or a bond with no walk-back.

Never write 0 for N/A. Zero means they have the organ and the count is zero. N/A means the pathway does not present.

## Correlate rule
Bond only when both sides are `SEALED` on the same pathway.
If Belleville is `N/A` and Toronto is `SEALED`, there is no Belleville–Toronto number to correlate. The bond is "pathway present on ceiling, absent on atom." That is a class marker, not a ratio.
If either side is `HOLE`, do not bond yet.

## What this does to the 444
Every municipality inherits every ceiling slot. Most of those slots will be `N/A`. That is correct. The frame stays the Toronto-width frame. The body stays itself.
