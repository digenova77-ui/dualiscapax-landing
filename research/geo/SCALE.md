# SCALE — four cities are the hard pass
2026-08-27 00:54 EDT
Law.

## Where the complexity actually sits
It sits in naming organs and species once.
Toronto vs Belleville vs Kingston vs Quinte West was expensive because that is where OPP vs muni, owned-out landfill, TCHC two books, CMA vs CSD, and CLOCK got cut.
The fifth city should mostly *hit* IDs. If it keeps inventing letters, the catalog failed the test — not the city.

## Matrix (this is the capture)
Axis 0: Body (`geo_id` + grain)
Axis 1: Organ.id
Axis 2 (optional): year / window
Cell: `HAS.stamp` always. `Cache.x` only when pulled.
That matrix *is* the world model. Not a news crawl.
Overlay is a view on two rows of the same column: STRONG / WEAK / ABSENT.
You can wipe axis 2 tonight. Axis 0×1 remains the plate.

## Why four done well makes the rest cheap
A new CSD is: add a row, stamp HAS from public pages, PULL only on the ask.
A new country is a new Body set + statute tokens. Same families if the organ grows. N/A if it does not. Do not fork MESH.
444 Ontario rows are 440 copies of the same walk plus the few species we have not met yet (regional police, two-tier lower-tier split). Those few get IDENTIFY. The rest is matrix fill.

## Exponential is fill, not law
Growth = more rows × same columns × recipes that already know the SRC kind.
Growth is not more activation functions.
If agents start adding node types, stop. The mesh already has six.

## World without slop
No Moscow row until a Russia frame writes Bodies.
No "global bike theft" mash. Same species, same definition, or refuse.
Capital-city flag, transit species, police species — stamp per row. The matrix keeps them honest.

## Agent rule
Treat the four as the exam.
Scale by adding rows and stamping existence.
Do not rebuild Dualis for the fifth town.
