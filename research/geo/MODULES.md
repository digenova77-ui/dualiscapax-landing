# Modules — one mission per send
2026-08-26 22:55 EDT

"Every business on Earth" is not a task. It is the sky.
One send = one module with a done-line. If it cannot finish in one working pass, it is still too big.

## Now (Ontario frame, no dollars)

| id | mission | done when |
|----|---------|----------|
| G0 | 444 ON municipality names into a text table | one row per MMAH name, no counts |
| G1 | Pass-1 count for **one** CSD (start: Belleville) from StatCan BR | one number + table cite + vintage |
| G2 | Repeat G1 for Quinte West | same shape |
| G3 | Pass-2 names for **one** Belleville slice (one street or one licence class) | NAMES rows, pointer or register labeled |
| G4 | Pass-3 NAICS/licence on those G3 rows only | sector filled or unsearched named |

G5 (public dollars on one named instance) does not start until that instance has G3.

## Later (still one send)

- G1 for the next municipality on QUEUE.md — never "do Ontario"
- One province's municipality list (G0-class), not that province's companies
- One country register *method* (what shelf exists), not that country's census

## Agent split on one module

Deep opens the one shelf. New drops echoes. Ingest writes the one table.
Do not run G1 and G3 in the same send unless G1 is already done.

## What this is for

A filled frame means when an onboarder later hands us their books, we already know which city they sit in, how dense that city is, and which names were public. Modeling gets easier because the empty slots are already named.
