# DAILY HANDOFF

Factory writes names into `research/`.
Clerk (us) copies only HOST_SAFE HTML into `cf-pages/`.
Website receives that folder when kitchen A or B runs.

Do not harvest straight onto apex.
Do not invent players or teams.
Do not pin Piñata first.

Today's ready tree is whatever `*.html` sits under `cf-pages/` on main.
Today's live tree is `research/LIVE-STATUS.md`.
Drift = ready and not 200.

After the joint welds, this file is the daily packet: what landed in git since yesterday is what the next deploy ships. No second pipeline.
