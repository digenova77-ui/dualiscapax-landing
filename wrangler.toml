name = "dualiscapax-web"
pages_build_output_dir = "."
compatibility_date = "2026-09-04"

# Static brochure only. No npm build.
# Dashboard: Workers & Pages → dualiscapax-web (or attach this repo)
# Build command: (empty)
# Root directory: /
# Output directory: /
