# DEPTH.NOW — bottoms this beat

**Stamp:** 2026-09-24T19:18-04:00
**Could this bless fake LIVE?** NO

Street this tick: Cloudflare DYNAMIC. `/` 200 DualisCapax — Iris cafe plate (root `index.html` SHA `cc649205…`). `/holographic-core/v2` 404 title Not found — DualisCapax. Encyclopedia 200. Pay 200.

## Roots (votes cannot close these)

### L-CAFE-APEX / L-HUD-404
**Bottom:** Apex serves root cafe `index.html`. HUD file lives at `cf-pages/holographic-core/v2/index.html` (nested) and says apex_overwrite false / apex `/` is not this page. Warehouse `cf-pages/index.html` ticker claims HOLOGRAPHIC APEX LIVE // CAFE PLATE ARCHIVED — that is L-WAREHOUSE-FAKE-LIVE.
**Next:** operator picks which file may become `/` before `confirm=DEPLOY`. A zip of the isolated HUD fights its own apex_overwrite:false. A zip of the warehouse lander would ship the fake-LIVE ticker. Nested holographic-core/v2 zipped as-is is L-HOLE-NO-INDEX class.
**Still need:** Pages Edit + zero-nest chosen-face zip + `confirm=DEPLOY` + trio `LIVE_HTTP_OK=1`.

### L-TOKEN-10000
**Bottom:** `EXECUTE_DEPLOY.sh` fail-closes without Pages Edit. Vote cannot mint token scope.
**Next:** operator scoped token. Factory stays closed.

### L-HOLE-NO-INDEX / L-SIGNING-SHA
**Bottom:** `README.SIGNING.md` still prints Drive SHA `3ce42dc1…` as payload. `EXECUTE_DEPLOY.sh` EXPECTED_SHA is that same hash. LAW.CURRENT already retired it as lander. Parent did not.
**Next:** stamp RETIRED on that payload line in a later paper pipe. Not this HTML pass.

### L-TILL-FIGHT
**Bottom:** `/pay` heading Live checkout + six buy.stripe.com links. Encyclopedia / hall / ice say closed. URLs exist; copy fights. Stripe Active vs Deactivated unconfirmed.
**Next:** independent Stripe state. Do not invent.

### L-LOOK-404
**Bottom:** `/look` 404. 404 plate names Look as if it exists. Door without route.

### L-ALACARTE-308
**Bottom:** earlier 308 loop; this beat also seen as 503. Drift named. Not a 200.

## ROOT-STILL-OPEN

All six IDs above. Depth named the roots. Depth did not close them.
