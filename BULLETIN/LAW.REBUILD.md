# LAW.REBUILD — strip noise, keep the kit, cycle into law

**Status:** STANDING 2026-09-24T19:12-04:00 after MILL-KEEP paper track.
**MOTION-ID:** REBUILD-LOOP
**Tally:** Harper / Lucas / Benjamin ACTION PASS · LIVE HOLD · TRIO RAN reuse (clock consumed)
**Clerk:** Grok
**This pass:** CLASSIFY + DEMOTE status. Zero file deletes.

Operator: strip everything unnecessary, keep real-time rebuild, build again deeper / faster / more granular, vote that loop into law. So be it.

So be it means the cycle is law. It does not mean HUD is LIVE.

## Rebuild kit (cannot strip)

Without these we cannot rebuild in real time. Deleting them is HOLE on HOST_SAFE.

- Isolated HUD DCLM-RTE-V2.0.4 + hostess-40
- `encyclopedia/` · `encyclopedia.html` · `06_ENCYC_*`
- `BULLETIN/` standing law + `SIGNAL.STORE.md` + `LOSS.LEDGER.md`
- `AGENT/DCLM-FLOOR-0.md` · `DCLM-WEB.md` · `DCLM-TWEAKS.md` · NEVER-COMMIT · PRIVILEGE
- `engine/dclm/` · `workers/` · `runtime/` · `js/`
- `cf-pages/` warehouse
- `BULLETIN/EXECUTE_DEPLOY.sh` waiting
- `research/` geo seals
- root `index.html` as L-CAFE-APEX street bytes until four GO conditions

## Demoted from production current (bytes stay)

- Cafe as current-face law
- Mill v2/v3 as current procedure
- Sidecars as law
- Extra trio maps as current (ZERO-TRIO remains the map)
- Warehouse APEX LIVE as fact
- PUBLISH-LAW supremacy · GH Pages as live rail · `07_SCRIPT__deploy_production.sh` as live rail
- Drive-as-script
- Root dumps as current-face: `01_CORE__*` · `02_ENG_SPEC__*` · `03_LEGAL__*` · `05_WEB__*` · `DualisCapax_Master_Index_V244.html` · `LIVE.md` · `COSMIC_FACTORY.md`
- Those files remain E-encyclopedia leaves / rebuild-optional. Not K-FACE.

## The cycle (law)

1. CLASSIFY every cell K / L / M / E / A / N
2. DEMOTE N and expired K from KEEP
3. REBUILD from kit only. Stamp the pass. Speed is the stamp delta. Fast smash is HOLE.
4. TRIO + KEEP-TEST
5. VOTE. Only a pass that passed becomes KEEP
6. Repeat on next operator order or HTTP change. Deeper / more granular each adopted pass

This pass is step 1–2. Rebuild-deeper is the next adopted pass, not a silent rewrite of `/`.

## This tick KEEP-TEST

K-KIT PASS · K-ENCYC PASS · K-MILL PASS · K-REBUILD-LOOP PASS procedure · K-FACE live FAIL 0 · K-TILL FAIL · K-WORKERS fence PASS

`LIVE_HTTP_OK` holographic = 0. L-CAFE-APEX stays LOSS.

## Verdict Twain²

We adopted the strip-and-rebuild loop as law. We did not burn the kit. The street is still Meet Iris.
Cycle is law. HUD is still 0.
