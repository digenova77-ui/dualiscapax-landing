# Boot reel drop

Place `iris-intro.mp4` at `/av/iris-intro.mp4` (and `cf-pages/av/iris-intro.mp4`).
8s. Muted after logo splash. Skip always. Replay with `?boot=1`.
Voice starts after BASE, not during the film.
