# DCLM State Authority Kernel (bridge)

`foundation: false`. Do not treat this directory as verified DualisCapax production.

One door: `StateAuthorityKernel.apply()`.
PROMOTABLE is derived. Evidence cannot carry PROMOTABLE.
Deny + immutable event + reasons otherwise.

Run: `python3 tests/test_endzone_kernel.py`
Scan leftover writers: `python3 scripts/scan_state_writers.py .`

See `docs/dclm/ENDZONE_REPORT.md`.
