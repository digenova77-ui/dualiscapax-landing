# DualisCapax Unity 4-model mesh

Staged consensus receipt. Env keys are accepted (`GEMINI_API_KEY`, `XAI_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`) but this V1 file does not call those APIs. Verdict `UNANIMOUS_PASS` is a local hash of four staged role cards.

Dockets land in `src/engine/ledgers/consensus/`.
Workflow: issues opened/labeled, or manual `workflow_dispatch`.
