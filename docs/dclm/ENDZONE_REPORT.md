# DCLM end-zone implementation report

Document control: `ED-DCLM-20260912-ENDZONE-BRIDGE-V1`  
Status: **BRIDGE**. `foundation: false`. Not self-verified. Ready for independent attack, not for promotion.

`dclm_truth_kernel.py` was not in the public tree. `dclm_singularity_kernel.py` is a projection, not an epistemic door.

Authoritative writes go through `src/engine/dclm/StateAuthorityKernel.apply()`.
PROMOTABLE is derived. Evidence cannot carry PROMOTABLE. Deny events are append-only.

Writer census of the live lander is INCOMPLETE. Stripe/Cloudflare/Pinata/hardware remain BLOCKED or UNRESOLVED.
21 process-local adversarial tests exist. The implementing agent wrote them. That is not independent verification.
