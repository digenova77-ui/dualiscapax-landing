# NO STATE = BROKEN

**Status:** Locked 26 Aug 2026 · **SUPERSEDES**  
**Operator law:** *You cannot have a state equal broken. It supersedes everything else we do — lock it in once you figure it out.*

**Priority:** This rule **outranks** all agent process docs except **Operator YES/NO** (which remains supreme law).  
If anything in residual-law conflicts with allowing a resting **broken** state, **this wins**.

Cross-ref: TDI-INVERTIBILITY · NO-TERMINAL-CANNOT · TOTAL-DUALIS-INTEGRATION · BRIDGEHEAD · YES-NO-IS-THE-LAW

---

## 1. Forbidden state

$$
\mathrm{State} \neq \mathtt{BROKEN}
$$

as a **resting**, **terminal**, or **accepted** operating state.

Agents may **detect** invertibility failure.  
They may **not** stop, ship, or report work complete while the system remains non-invertible / non-whole.

---

## 2. What “broken” is allowed to be

| Allowed | Forbidden |
|---------|-----------|
| Transient **diagnostic flag** during audit | Final status of a work unit |
| Trigger for **immediate repair** | Excuse to halt under NO-TERMINAL-CANNOT |
| Input to bridge protocol | Accepted TDI theater with missing \(\mathcal{R}\) |

$$
\mathtt{DETECT\_BROKEN} \;\longrightarrow\; \mathtt{REPAIR\_UNTIL\_INVERTIBLE} \;\longrightarrow\; \mathtt{WHOLE}
$$

Never:

$$
\mathtt{DETECT\_BROKEN} \;\longrightarrow\; \mathtt{STOP}
$$

---

## 3. Supersession order

```
0. Operator YES / NO          (supreme)
1. NO STATE = BROKEN          (this file — supersedes process below)
2. NO-TERMINAL-CANNOT
3. TDI invertibility + absolute expression
4. Bridge / BIND / encompass / buckets / …
```

---

## 4. Repair mandate (when diagnostic broken fires)

1. Reconstruct missing \(\mathcal{R}\) fields (handoffs, abilities, friction terms, law bits).  
2. Or recompute forward from known originals and rewrite ledger.  
3. Or open bridge protocol we own until dual-TOS path is whole.  
4. Re-run inverse; only then continue other goals.

**Until inverse succeeds, repair outranks encyclopedia, Bond, and convenience ships that claim TDI.**

---

## 5. One line

**There is no allowed state equal to broken — only detect, then repair until whole; this supersedes everything else we do except Operator YES/NO.**

**Last update:** 26 Aug 2026 — SUPERSEDES locked.
