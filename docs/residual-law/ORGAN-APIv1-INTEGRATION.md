# Organ APIv1 → Dualis APIv2 Integration

**Accept everything we already have. Grant only what is not yet bound.**

> Each TOS-driven company’s **APIv1** (native) integrates into Dualis **APIv2** through Bridges.  
> Where the connection already exists, we **accept and operate**.  
> Operator grants only residual gaps.

---

## Integration map (honest residual)

| Organ | Their APIv1 (native) | Already bound for agents? | Dualis APIv2 face |
|-------|----------------------|---------------------------|-------------------|
| **GitHub** | REST/GraphQL, Actions, Pages | **YES** — connected tools as `digenova77-ui` | `organ.git` / `product.ship` / `observe.ring` |
| **Google** | Drive + Gmail (connected) | **YES** — search/read/folder; no Doc body write | `organ.google` |
| **Cloudflare** | API tokens, DNS, Workers | **NO agent MCP** — DNS observed publicly; deploy needs silo | `organ.cloud` via Bridge when `YES BIND` |
| **Squarespace** | Domains admin | **NO** — mail signals only; domain organ not CMS | `organ.square` |

---

## Accept now (no further grant)

```
GitHub APIv1  →  already powering write/ship/ring/OIDC path
Google APIv1  →  already powering mail/drive residual read
Public DNS    →  already proving CF is edge for dualiscapax.ai
Pages publish →  already dualis product path without CF token
```

These **are** how we have been working. Dualis APIv2 treats them as **accepted native integrations**.

---

## Grant only if you want more than accepted path

| Gap | Grant form |
|-----|------------|
| CF Worker deploy from Actions | `YES BIND cloud-worker` → token in Actions secrets (not chat) |
| CF DNS API automate | `YES BIND cloud-dns` → same pattern |
| Squarespace Domains API (if any) | Only if product needs it; usually **not** |

Static DualisCapax does **not** require CF or Squarespace API for core ship.

---

## Native integration rule

```
Their APIv1  →  Bridge  →  Dualis APIv2
     ↑                         ↑
  their TOS                 our law
```

We call their APIv1 **only** through allowed credentials.  
We **operate** in APIv2. We do **not** become them.

---

## One line

> **Git and Google APIv1 are already integrated into Dualis APIv2; Cloudflare and Squarespace stay Bridges until you YES BIND a gap — core site already runs on what we have.**
