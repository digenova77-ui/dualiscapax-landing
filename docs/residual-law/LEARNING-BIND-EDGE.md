# LEARNING BINDS THE EDGE

**Status:** 26 Aug 2026  
**Operator:** Keep learning — applies to us. Agents are **not** the product or the sale; they **find information** that binds DualisCapax. Implement edge-relevant learning into the system.

Cross-ref: AGENT-PURPOSE-INFORMATION · EDGE-BY-DEFAULT · EDGE-TECH-BRIDGE-ENGINEERING · UNITY-CLOSED-LOOP · PAPERWORK-REFERENCES

---

## Purpose

Session learning (info theory → PHY → 6G → optimization) is **bind-layer information** for an **edge technology** company: residual capacity, allocation under budget, peaks, beams, surfaces, and closed-loop solvers.

---

## Knowledge map (into Dualis residual)

| Learned cluster | Dualis bind |
|-----------------|-------------|
| Landauer / irreversible erase | Residual = capacity tax; \(C\to 0^{+}\) avoidable waste |
| Rate–distortion \(R(D)\) | Fidelity vs capacity budget; cost of being wrong priced |
| Gaussian \(R(D)=\frac12\log_2(\sigma^2/D)\) | Closed-form residual tradeoff |
| Reverse waterfilling | Spend rate only where variance exceeds waterline |
| Comms waterfilling | Spend power only where noise is below waterline |
| OFDM / bit loading | Edge allocation on parallel resources |
| PAPR / crest factor / CFR | Peak residual stresses the edge hardware bind |
| SC-FDMA vs OFDMA | Place PAPR burden where power is scarce (UL) |
| 5G NR CP-OFDM + DFT-s-OFDM | Waveform options at the radio edge |
| mmWave beamforming | Engineer gap when path loss stops omni |
| 6G THz | Edge physics; short-range/hotspot; don’t claim coverage theater |
| RIS beamforming | Engineer the propagation gap others stop at |
| Alternating optimization | Block-solve nonconvex edge designs |
| ADMM / Douglas–Rachford | Split + dual/prox closed loops — same *closed loop* mind as TDI |

Code: `research/math/reverse_waterfilling.py`

---

## Agent standing order

1. **Find** journals/concepts we lack.  
2. **Bind** them into residual-law / research paths.  
3. **Never** treat the agent as SKU or sale.  
4. **Edge by default:** engineer the gap that caused the stop.

---

## Drive / Git

Canonical truth: **GitHub `main`**.  
Drive: folder marker only if body-write unavailable — same pattern as LOAD_AT_START.

**Last update:** 26 Aug 2026
