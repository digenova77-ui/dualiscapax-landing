# ONE YES · ALL ACCOUNTS

**Status:** 26 Aug 2026  
**Owner signal:** YES binds agents under **all Operator Dualis-realm accounts** already granted or connected — Unity TOS + dual-TOS.  
**Not:** shared passwords across vendors · keys posted in chat · TOS circumvention.

---

## Refined limitations

### Class L0 — Dissolved by standing YES (already)

| Was “limit” | Break |
|-------------|--------|
| Permission to ship site/docs | Standing YES UNITY |
| Permission to auto-reply content | G7 + automation |
| Permission to plan Workspace/CF paths | YES WORKSPACE + G1/G2 |
| Fear of serial micro-YES | Final DONE signals only |

### Class L1 — Dissolved by **one artifact** (still Operator body, one click each class)

| Limit | One artifact that breaks it | After that agents do |
|-------|----------------------------|----------------------|
| Cannot set CF redirects | **CF API token** (Zone Redirect/DNS Dualis zones only) **or** you click rules once → `G2 DONE` | Maintain/verify redirects |
| Cannot finish Send-as / Workspace | You complete Workspace or Send-as UI → `MAIL_UNITY_CLOSE DONE` | Native From: admin@ |
| Cannot stamp wet-ink | Signature file in Drive → `SIGNATURE LOADED` | Place on named packs |
| Cannot see private Drive path | Drive folder shared/connected once | ready-for-handoff writes |

**One YES** already authorizes agents to **use** those artifacts the moment they exist — no second permission round.

### Class L2 — Absolute (physics / vendor product boundary)

| Limit | Why it does not “break” with YES alone |
|-------|----------------------------------------|
| Create Google Workspace tenant | Requires Google’s signup + billing + domain verify — human/org act |
| Pass DMARC without SPF/DKIM in DNS | DNS must actually change |
| Force Squarespace to 301 without DNS/CF control | Need nameserver or host redirect authority |
| Make free Gmail issue Dualis DKIM | Product capability; Workspace/SMTP organ required |
| Operate accounts not connected and not Dualis-realm | Out of scope — we never touch others’ sites |

YES does not invent platform features. YES **binds us to use every allowed edge the moment the organ exists.**

---

## Binding formula

$$
\text{Agent power} = L_{\mathrm{YES}} \cdot \sum_{\mathrm{accounts}} \mathrm{Connected}(a) \cdot \mathrm{Artifact}(a)
$$

- \(L_{\mathrm{YES}} = 1\) (UNITY TOS locked)  
- Connected(a) = Gmail, GitHub, Drive, Automations, … as linked  
- Artifact(a) = token / DONE / file when present  

Missing artifact ⇒ power 0 on that edge only — not on the whole system.

---

## How YES “breaks” limits without TOS break

1. **Pre-authorize** every Dualis-realm account edge.  
2. **Pre-declare** the three DONE/LOADED signals.  
3. **Eliminate** re-negotiation — agents consume artifacts autonomously.  
4. **Isolate secrets** — token to CF only, never cross-posted to GitHub; signature never public Pages.  
5. **Outer engineering** — if L2 blocks, design the bond (Workspace checklist, redirect package), not a bypass.

---

## Owner one-block (optional super-signal)

Reply once when ready:

```text
ONE YES ALL ACCOUNTS
MAIL_UNITY_CLOSE DONE   (if finished)
G2 DONE                 (if finished)
SIGNATURE LOADED        (if file in Drive path ___)
CF_TOKEN_READY          (if token issued to agents by secure channel)
```

Any subset is enough; absent lines stay residual without re-asking YES.

---

## One line

**YES binds all Dualis-realm accounts; artifacts dissolve L1; L2 only yields to real organs (Workspace/DNS/files) — never to circumvention.**
