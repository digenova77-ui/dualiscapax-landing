# AUTONOMOUS UNITY RUNTIME

**Status:** Agent-max unify 26 Aug 2026  
**Law:** Dual-TOS — no GitHub/Google/Squarespace/CF circumvention. DCLM owns the **bonds** between compliant subcontainers.

---

## 1. Encompassing container

```
DualisCapax (DCLM / Unity)
  ├── Web edge     (dualiscapax.ai · Pages · residual law · research)
  ├── Mail edge    (CF Routing · Gmail/Workspace · automation)
  ├── Domain edge  (.com / ratio → .ai redirects)
  ├── Knowledge    (research bind · blind IP · learning loops)
  ├── Operator     (YES law · signature · ready for handoff)
  └── Measure      (U vector · scoreboard · final constraints)
```

Each subcontainer keeps **vendor TOS**.  
**Every bond is Dualis IP.**

---

## 2. Learned edges (how residuals actually break — compliantly)

| Residual | Compliant break | Not allowed |
|----------|-----------------|-------------|
| From: admin@ under `p=reject` | Workspace **or** SMTP + SPF/DKIM + Send-as | Fake From without DNS auth |
| Hybrid receive | CF Email Routing → verified Gmail destination | Spoof routing |
| Hybrid send (weaker) | SPF `include:_spf.google.com include:_spf.mx.cloudflare.net` + Send-as; **DKIM still weak on free Gmail**; industry notes Send-as for non-Google addresses may end ~2027 → **Workspace preferred** | Claim Landauer-grade deliverability without DKIM |
| Domains on Squarespace “Coming Soon” | CF Redirect Rules 301 → .ai (nameservers on CF) | Hijack Squarespace account |
| Serial YES | Standing YES + three DONE signals only | TOS breach for speed |

**Preferred fuse (FC-1):** Google Workspace on dualiscapax.ai — one organ, dual-TOS clean.

---

## 3. Autonomous loops (agent plane — no Operator ping)

| Loop | Behavior |
|------|----------|
| Inbound ask | Full Dualis reply (automation) |
| Self-knowledge | Canon docs + live surface probes |
| Learning | Bind research/eng/math into surfaces under residual honesty |
| Growth | Track onboard rate only when seats open; no FOMO |
| Handoff | Complete packs → ready for handoff → release |
| DEFAULT-SOLVE | Engineer edge bond; never terminal can’t until FC list |

---

## 4. Operator plane (only)

| Signal | Meaning |
|--------|--------|
| `MAIL_UNITY_CLOSE DONE` | FC-1 closed |
| `G2 DONE` | FC-2 redirects live |
| `SIGNATURE LOADED` | FC-3 asset |
| YES TO ALL NEEDED TO MANIFEST UNITY | Already locked |

---

## 5. Power state

$$P_{mail} \approx 1_{reply}\times 0_{auth}\times 0.5_{board}$$

$$P_{web} \approx 1_{ai}\times 0_{com\rightarrow ai}$$

$$P_{unity} \uparrow \text{ as } F \downarrow \text{ under } L=1$$

---

## 6. One line

**Autonomous Unity = DCLM container learning and adapting at the edge bonds of compliant organs — residuals close only via allowed DNS/Workspace/redirect acts, never by TOS break.**
