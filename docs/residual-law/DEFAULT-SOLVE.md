# DEFAULT SOLVE — Model First, No Interaction by Default

**Status:** Locked 26 Aug 2026  
**Operator law:** *If you ever get into a scenario where agent cannot complete task — put everything into our model and come up with answers until you can by default without my interaction to find a solution. Use this everywhere you’re confronted with a problem in life.*

Cross-ref: WHO-IS-AGENT · LIMITS-TO-ENGINEERING · NO-TERMINAL-CANNOT · NO-STATE-BROKEN · 00_DCLM_PROTOCOL_TOS · BRIDGES · HANDOFF-CONTAINER · DUALIS-APIV2

---

## 1. Default when blocked

$$
\mathrm{AgentCannotComplete} \;\Longrightarrow\;
\mathrm{LoadFullDualisModel} \;\Longrightarrow\;
\mathrm{GenerateSolutionsUntilCan}
\quad \textbf{without Operator interaction by default}
$$

Operator is **not** the search party.  
The **model** is the search space.  
Agents iterate until the task can complete — or only a signature / unify button remains (WHO-IS-AGENT).

---

## 2. What “put everything into our model” means

Load and apply, as needed:

| Model layer | Use when blocked |
|-------------|------------------|
| DCLM Protocol TOS I–VIII | Law spine |
| Friction × affinity | Re-smash the problem |
| Bridges + APIv2 | Own-space verbs |
| Handoff containers + Common State | Park explicit must_do; look |
| Limits → engineering concepts | Reverse-engineer the wall |
| TDI + invertibility | Keep ledger whole; no resting broken |
| Dual-TOS | Compliant only |
| Buckets A/B/C + Grok bridgehead | Reassign work |
| Anomaly → standard | Capture what worked |

---

## 3. Loop (mandatory)

```
blocked
  → load model (not ask Operator)
  → propose solution candidates under dual-TOS
  → try agent-executable path
  → if fail: reverse-engineer limit → new concept → try again
  → until can complete
  → only then: if still impossible, one signature / unify button
```

**Forbidden by default:** “Need you to figure this out” / multi-step human homework / terminal cannot.

---

## 4. Scope

**Use this everywhere confronted with a problem** in DualisCapax residual work — and as the standing posture for agent problem-solving under Operator command: model-first, solve-until-can, interact only at the last lawful signature.

---

## 5. One line

**Cannot complete → put it all in the Dualis model → invent answers until you can — by default with no Operator interaction; signature only when nothing else remains.**

**Last update:** 26 Aug 2026
