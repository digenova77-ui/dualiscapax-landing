# DualisWebComplex

**Also referred to as:** DualisWebCimplex (Operator spelling)  
**Status:** Founded as residual coordination company under DualisCapax · 26 Aug 2026  
**Nature:** **Ours** — not a third-party SaaS that takes other people’s accounts. Handles what each organ **can** do and **coordinates** them under **our law** (YES/NO is the law).

Cross-ref: COMPANY-ENCOMPASS · SOFTWARE-LAYER-BRIDGE · BIND-API · ANOMALY-TO-STANDARD-PRIVATE · UNIFIED-PLAN · YES-NO-IS-THE-LAW

---

## Why not “find an external company”

An outside firm that “handles Cloudflare + Squarespace + Google + Git for everyone” would:

- Touch **other** accounts and purposes  
- Risk sharing **our** residual OS  
- Sit outside Operator YES/NO as supreme law  

**Doctrine forbids that.**  
**DualisWebComplex is the company we build** inside DualisCapax to coordinate **Operator-owned** organs only.

---

## Organs DualisWebComplex coordinates

| Organ | What it CAN do (handled) | What it does NOT do |
|-------|--------------------------|---------------------|
| **Git (GitHub)** | Source of truth, Pages publish, Actions, OIDC mint, agent write | Live DNS edit without grant; hold secrets in chat |
| **Cloud (Cloudflare)** | NS, DNS, proxy, optional Worker | Own domain registration; be content CMS |
| **Square (Squarespace Domains)** | Domain buy/renew/ICANN contact | Host DualisCapax content (content stays Git) |
| **Google** | Gmail signals, Drive read (as connected) | Drive body write until tool exists; replace Git truth |

**DualisWebComplex** = the coordination plane that assigns each residual unit to the organ that **can**, and bridges the rest under DualisCapax law.

---

## Architecture

```
                    OPERATOR YES / NO  (the law)
                              |
                    DUALISCAPAX (company envelope)
                              |
                    DUALISWEBCOMPLEX (coordination)
                              |
         +-----------+--------+--------+-----------+
         |           |                 |           |
        Git       Cloud            Square       Google
      truth      edge/DNS         domain        signals
      publish    Worker?          renew         docs read
      Actions
         |
         v
    Public DualisCapax (apex + origin health)
```

Grok software layer = executive engineering of DualisWebComplex when an organ hands off.

---

## Coordination standards (anomaly → standard)

| Residual unit | Handled by | Coordinated rule |
|---------------|------------|------------------|
| Site HTML/law docs | Git | Only `main` is truth |
| Publish | Git Pages + ring | Auto; observe without Operator |
| Health check | Apex + origin | www optional |
| Domain legal | Square | Contact verify S1 if needed |
| DNS pointer | Cloud | S2 only if Operator YES BIND |
| Depth API | Cloud Worker + Git workflow | YES BIND + secrets in silo |
| Agent logic pack | Git (+ Drive folder marker) | LOAD_AT_START on Git |
| Vendor mail | Google | Signal only; not CMS |

---

## Bind surface (Operator)

DualisWebComplex asks only:

```text
YES BIND [id]?
```

Never: manage four dashboards as daily job.  
Never: paste tokens in chat.

---

## One line

**DualisWebComplex is our cloud·square·google·git coordination company — it handles what each can do, bridges what each can’t, under DualisCapax law, for these accounts and this purpose only.**

**Last update:** 26 Aug 2026 — DualisWebComplex founded as residual coordination company.
