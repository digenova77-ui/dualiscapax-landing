# UNIFIED PLAN — Friction × Affinity Until Smash

**Status:** 26 Aug 2026  
**Method:** Attack with **friction** (what breaks, wastes, confuses), then **affinity** (what belongs together), repeat until the four integrated entities and the product are **one system** — better than knowing each tool alone.

**Entities smashed:** GitHub · Cloudflare · Squarespace Domains · Google (connected)  
**Product:** DualisCapax residual law · site · encyclopedia · Bond · Fuel  
**Command:** Operator YES/NO/GOLD · Internal TOS · Build-until-compliant

---

## Cycle 1 — Friction (what was burning residual)

| Friction | Symptom |
|----------|--------|
| F1 | www 522 treated as “whole site down” |
| F2 | Agents re-diagnosing domain every session |
| F3 | Path contention (A+C same indexes) |
| F4 | Expecting Operator to edit DNS/HTML daily |
| F5 | Secrets imagined in chat |
| F6 | Drive write assumed; connector read-only for body |
| F7 | OIDC mistaken for leaf-write speedup |
| F8 | Squarespace mail ≠ “host site on Squarespace” |
| F9 | CLOSED work reopened by human fumbling |
| F10 | “Can’t” before origin/apex/repo probed |

**Friction score if unaddressed:** high Landauer process residual (rework, panic, undo).

---

## Cycle 1 — Affinity (what belongs together)

| Cluster | Members | One job |
|---------|---------|--------|
| **Truth** | GitHub `main` | Only content source |
| **Publish** | Pages + residual-ring | Auto on push |
| **Prove live** | Origin + apex | Health measure |
| **Own name** | Squarespace Domains | Buy/renew/verify only |
| **Edge DNS** | Cloudflare NS/zone | Pointers; optional Worker |
| **Operator inbox** | Gmail/Drive | Signals + docs read |
| **Command** | Internal TOS + GOLD + AUTHORIZE | Least human surface |
| **Build law** | Buckets A/B/C + CANT-LOOP + Build-until-compliant | How agents work |

---

## Cycle 2 — Friction again (deeper)

| After Cycle 1 | Remaining friction |
|---------------|-------------------|
| F1' | Operator still *might* touch CF and undo |
| F2' | Worker/depth not live until first-party grant |
| F3' | Multi-agent false dedup on writes |
| F4' | Encyclopedia not full density yet |
| F5' | Bond nav not uniform on every spine page |

---

## Cycle 2 — Affinity again (tighter smash)

```
                    OPERATOR COMMAND
                 (YES/NO/GOLD/HOLD/CHECK)
                           |
         +-----------------+-----------------+
         |                 |                 |
    CONTENT TRUTH      EDGE POINTERS    IDENTITY/MAIL
    GitHub main        Cloudflare DNS   Squarespace
    Pages publish      optional Worker  Gmail/Drive
    residual-ring      apex primary     verify only
         |                 |                 |
         +--------+--------+--------+--------+
                  |
           PUBLIC DUALISCAPAX
        (apex + origin = product)
                  |
         encyclopedia + Bond + Fuel
              under DCLM
```

**Smash result:** Four vendors are not four projects. They are **four residual organs of one body**.

---

## Cycle 3 — Friction (attack the gaps in the smash)

| Gap | Friction attack |
|-----|-----------------|
| www | Declare non-critical; never block GOLD on www |
| Depth API | First-party Worker grant when Operator says YES S3/S4 — until then Open residual only |
| Agent conflict | Hard buckets + LEAF-LIVE serial |
| Human undo | CLOSED C1–C10 + REVOKE only explicit |
| Missing tool | Build-until-compliant (GitHub agent file, not fake Drive write) |

---

## Cycle 3 — Affinity (what is better than “knowing capabilities alone”)

**Before (isolated knowledge):**  
“We can write GitHub. CF is DNS. Squarespace sold the domain. Drive can read.”

**After (unified plan):**

| Capability alone | Unified system behavior |
|------------------|-------------------------|
| GitHub write | Continuous truth organ under buckets |
| Pages | Automatic body of publish |
| residual-ring | Immune system (observe without Operator) |
| Apex/origin | Sensory system (health) |
| Cloudflare | Skeleton pointers — not the brain |
| Squarespace | Legal name plate of the domain |
| Gmail | Nerve for domain/vendor signals |
| TOS + GOLD | Nervous system of command |
| Build-until-compliant | Healing rule when a path is blocked |

**Better than before:** Agents optimize **the organism**, not a checklist of APIs.

---

## THE ONE PLAN (operating system)

### Layer A — Always on (no Operator)

1. Agents ship under A/B/C buckets to `main`.  
2. Pages + residual-ring run.  
3. Health = apex + origin + ring green.  
4. CLOSED residuals stay sealed.  
5. Friction events → affinity re-map → rebuild-until-compliant if needed.

### Layer B — Operator (seconds, not projects)

| Phrase | Effect |
|--------|--------|
| `I AUTHORIZE AGENTS` | Seal command + internal TOS |
| `YES` / `NO` | Single ask |
| `GOLD LAUNCH NOW` | Freeze public-breaking change; depth+fixes only |
| `HOLD` | Pause experiments |
| `CHECK` | We investigate |
| `YES S1…S4` | Rare first-party grants |
| `REVOKE AGENT WRITE` | Hard stop writes |

### Layer C — First-party grants (only when plan needs them)

Operator creates/grants under **their** CF/GitHub/Stripe/xAI — never third-party breach.  
Agents consume via silos. If grant missing → ship Open residual; rebuild depth later compliantly.

### Layer D — Attack rhythm (repeat forever)

```
FRICTION pass  → list waste, undos, false can’ts, host confusion
AFFINITY pass  → rebind organs (truth/publish/prove/own/edge/command)
SMASH pass     → one diagram, one health metric, one command language
SHIP pass      → main + ring + measure apex/origin
```

Do this **over and over** each session — not once.

---

## Success metric (unified, not per-tool)

```
score = (apex_ok ∧ origin_ok ∧ ring_green ∧ no_secret_leak ∧ CLOSED_intact)
        × (encyclopedia_depth_progress + Bond_consistency)
        ÷ (Operator_minutes_spent + contention_rejects + false_cant_events)
```

Maximize score. Individual “we can call API X” is only an input, not the score.

---

## What this plan is better than

| Old | New |
|-----|-----|
| Four separate admin chores | One organism map |
| Capability inventory | Friction×affinity operating rhythm |
| Human as mechanic | Human as commander |
| Panic on www | Health on apex+origin |
| Stuck = can’t | Stuck = rebuild until compliant |
| Trust by watching every commit | Trust by CLOSED list + ring + GOLD |

---

## Immediate execution under this plan

1. Keep shipping leaves/Bond/structure on `main` (Layer A).  
2. Await Operator seal phrases when ready (Layer B).  
3. No CF/Squarespace daily work unless YES S#.  
4. Next friction pass after each major ship: did we create Operator load or undo risk? If yes, affinity-fix the process.  
5. Encyclopedia density + Bond sameness = primary product residual while edge organs stay stable.

---

## One line

**Friction finds the waste; affinity binds GitHub·Cloudflare·Squarespace·Google into one DualisCapax body; smash and ship on apex+origin under Operator command — then repeat until the system is smarter than any single integration.**

**Last update:** 26 Aug 2026 — Unified plan locked.
