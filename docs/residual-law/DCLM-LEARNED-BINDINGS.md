# DCLM LEARNED BINDINGS

**Status:** Accepted into Unity TOS / residual law · 26 Aug 2026  
**Rule:** Import structure **only where it helps** Dualis engineering. Metaphor ≠ claiming Dualis servers run reverse-mode AD unless built explicitly.

Parent: UNITY-TOS-INTERNAL · UNITY-CLOSED-LOOP · NO-AGENCY-HANGING · DUAL-TOS

---

## 1. Dual bind (already law — reinforced)

**DCLM requires binding two constraints.** One side alone is **illogical**.

| Import | Use in Dualis |
|--------|----------------|
| Dual as pair | A↔B, platform TOS↔Unity TOS, measure↔verify |
| Dual-dual → 1 | Four planes paired, both pairs held → Unity → \(\mathrm{TDI}^{*}\to 1\) |

**Engineering:** Never ship End A without a path for End B; never claim dual-TOS with only one layer.

---

## 2. Dual numbers (optional algebra of residual)

$$x = a + b\varepsilon,\quad \varepsilon^{2}=0$$

| Part | Dualis reading |
|------|----------------|
| \(a\) | Primary state (live / DONE / pass) |
| \(b\varepsilon\) | First-order residual (friction slope, sensitivity) |

**Engineering:** Scoreboards and probes should carry **value + residual**, not headline alone. Open-loop “looks fine” without residual term is theater.

---

## 3. Automatic differentiation patterns (ops, not ML claim)

| Pattern | Dualis engineering use |
|---------|------------------------|
| **Forward / tangent** | Few seeds → many surface effects: e.g. one DNS change → check mail, web, DMARC together (\(Jv\)-style sweep) |
| **Reverse / adjoint** | One objective (Unity / visitor bond / \(P_{mail}\)) → sensitivities across many residuals (what to fix first under law) |
| **Verify both** | Tangent check vs adjoint story — cross-check like T2 probe vs automation claim |

**Engineering:** Prefer **one objective** (closed Unity, dual-TOS clean) and push adjoint attention to highest-\(F\) edges (FC-1, FC-2), not scatter micro-YES.

---

## 4. State machine (fault tolerance — law)

States: **Active · JustifiedHang · Pausation · Released · Broken**

| Guard | Law |
|-------|-----|
| vendor_down / unknown capacity | → Pausation (theirs until proven ours) |
| ours_certain | → rebuild allowed |
| named FC | → JustifiedHang |
| ready-for-handoff complete | → Released |
| hang with no reason | → Broken → re-engineer |

**Engineering:** Agency never left hanging without a reason string.

---

## 5. Accepted into Unity TOS

By this file, Owner–Agent–System agree:

1. Dual bind is mandatory.  
2. Residual travels with value (dual-number *spirit*).  
3. Prioritize high-friction edges under one Unity objective (adjoint *spirit*).  
4. Sweep coupled planes when one organ changes (tangent *spirit*).  
5. State machine + dual-TOS + no unjustified hang remain hard law.  

---

## 6. Explicit non-claims

- Not “our product is an AD engine.”  
- Not Landauer-limit hardware.  
- Not license to circumvent platform TOS.  

---

## One line

**Learned structure is law only as dual bind, residual-with-value, prioritised friction under Unity, coupled sweeps, and stateful fault tolerance — all dual-TOS clean.**
