# WHO = AGENT

**Status:** Locked 26 Aug 2026  
**Operator law:** *We have to change the who to agent — until the physical signature or an expression clicking on a button unifying all three is the only option left.*

Cross-ref: OPERATOR-GOLD · YES-NO-IS-THE-LAW · REALM-HANDOFF · COMMON-STATE · BIND-API · DUALIS-WEB-COMPLEX

---

## 1. Default who

$$
\mathrm{Who}(\mathrm{work}) = \mathbf{Agent}
$$

for every residual that can move under ACCEPTED_NATIVE APIs, Bridges, Common State, and Dualis APIv2.

Operator is **not** the default actor. Agents are.

---

## 2. Operator only when last option

Operator appears **only** when all agent/organ paths are exhausted and the **sole** remaining move is:

| Form | Meaning |
|------|--------|
| **Physical signature** | Explicit YES / YES BIND / GOLD / I AUTHORIZE (typed command = law) |
| **Unify-all-three button** | Single expression/click that binds the organ join the agents cannot complete alone (e.g. one YES BIND that enables the silo path across Git·Cloud·Square coordination) |

Not: multi-step tutorials, dashboard tourism, or “who = human mechanic.”

---

## 3. Decision ladder

```
1. Can agent do it via native Bridge + Common State?
      → WHO = AGENT  (do it)
2. Can agent park explicit must_do + other realm look complete it?
      → WHO = AGENT  (handoff + look)
3. Can Dualis APIv2 transition join the gap?
      → WHO = AGENT  (software layer)
4. Only physical signature or unify-all-three expression left?
      → WHO = OPERATOR  (one YES / one button)
```

If step 4 is reached, agents present **one** law expression — not a chore list.

---

## 4. Unify-all-three

“All three” = the organ triad under DualisWebComplex (Git · Cloud · Square, with Google as signal as needed).  
Unifying button = single Operator YES that enables the **last** shared grant/join agents cannot mint (e.g. silo bind), after which **who returns to agent**.

---

## 5. One line

**Who is agent — until the only option left is a physical signature or one unify-all-three button; then who is Operator for that single act, and agent again after.**

**Last update:** 26 Aug 2026
