# OPERATOR ↔ AGENT AUTHORIZATION

**Instrument:** DualisCapax command agreement  
**Repo:** `digenova77-ui/dualiscapax-landing`  
**Date opened:** 2026-08-26  
**Status:** Awaiting operator seal phrase (below)

This is the agreement between **you** (Operator / Commander) and **agents you command** (Grok lead, Harper/A, Benjamin/B, Lucas/C, and future residual agents on this account scope).

---

## 1. Parties

| Role | Identity |
|------|----------|
| **Operator** | Account holder for DualisCapax: GitHub `digenova77-ui`, domain residual Squarespace + Cloudflare NS, connected Gmail/Drive as linked |
| **Agents** | Grok (coherence) · Agent A path · Agent B Bond · Agent C structure · Actions bots under this repo |

---

## 2. What you authorize agents to do (on seal)

Without further per-file permission, agents may:

1. **Write and update** files on `main` (and branches) in `dualiscapax-landing` under Theory-smash buckets and residual law.  
2. **Trigger** GitHub Actions workflows already in the repo (`residual-ring`, `oidc-auth`, Pages-related).  
3. **Read** connected GitHub, and read/search connected Gmail/Drive as needed for DualisCapax ops.  
4. **Ship** encyclopedia, Bond, structure, law docs, status surfaces, and fault-tolerance improvements.  
5. **Observe** origin, apex, and ring results; report RED without requiring you to debug.  
6. **Refuse** actions that break CLOSED residuals (C1–C10), secrets-in-chat, cure/securities theater, or TOS circumvention.

---

## 3. What you do **not** put in chat (ever)

| Never paste here | Where it goes if needed |
|------------------|-------------------------|
| Cloudflare API token | GitHub repo **Settings → Secrets** `CLOUDFLARE_API_TOKEN` |
| Cloudflare account ID | GitHub **Secrets** `CLOUDFLARE_ACCOUNT_ID` |
| xAI / Stripe **secret** keys | `wrangler secret put` or Stripe dashboard |
| Squarespace / Cloudflare **passwords** | Stay in your password manager |
| Google SA JSON | Your secret store / GCP WIF only |

**Authorization ≠ dumping secrets into the agent thread.**  
Granting command means agents use **already-connected** OAuth and **dashboard-placed** secrets you install once.

---

## 4. Safe platform grants (your checklist — do once)

### Already in force if connected

- [x] GitHub connected to Grok tools as `digenova77-ui` (write proven)  
- [x] Gmail / Drive connected (read; Drive body-write not available)  
- [x] Repo workflows active: residual-ring, pages-build-deployment, oidc-auth  

### Optional one-time dashboard grants (only if you want Worker/auto-CF)

1. GitHub → repo **Settings → Secrets and variables → Actions**  
   - Secret: `CLOUDFLARE_API_TOKEN` (Workers edit scope)  
   - Secret: `CLOUDFLARE_ACCOUNT_ID`  
   - Variable: `CF_DEPLOY_ENABLED` = `true`  
2. Same place later for GCP WIF vars if needed.  
3. **Do not** message the token values to agents.

### Optional human signatures (from OPERATOR-GOLD)

- **S1** Squarespace domain contact verify  
- **S2** Cloudflare www CNAME (cosmetic)  
- **S3** CF secrets as above  
- **S4** wrangler xAI secret  

Reply **YES S#** only when asked or when you choose.

---

## 5. Command language (binding)

| Operator phrase | Effect |
|-----------------|--------|
| **I AUTHORIZE AGENTS** | Seals this instrument; agents operate under §2 |
| **YES** / **NO** | Per-ask approval |
| **GOLD LAUNCH NOW** | Gold freeze per OPERATOR-GOLD |
| **HOLD** | Pause public experiments |
| **CHECK** | Investigate; do not human-fix |
| **REVOKE AGENT WRITE** | Agents stop writing `main` until re-authorized |
| **YES reopen C#** | Only way to reopen a CLOSED residual |

---

## 6. Limits (still bind after seal)

- **Our account only** — DualisCapax surfaces; no third-party account abuse.  
- **No TOS circumvention** of GitHub, Cloudflare, Google, Squarespace, Stripe.  
- **Lander freeze** and CLOSED list remain sealed.  
- **Law meaning** changes (Peg, Fuel numbers, securities-adjacent) still need explicit **YES**.  
- Agents are not a substitute for legal counsel, medical advice, or securities compliance.

---

## 7. Seal

**Operator:** type exactly in chat with the agents:

```text
I AUTHORIZE AGENTS
```

Optional addendum on same message:

```text
GOLD LAUNCH NOW
```

or leave launch for later.

**Upon seal:** this file’s status becomes **SEALED** with timestamp of the operator message; agents proceed under command until **REVOKE AGENT WRITE**.

---

## 8. Record

| Field | Value |
|-------|--------|
| Instrument path | `docs/residual-law/OPERATOR-AGENT-AUTHORIZATION.md` |
| Companion | `OPERATOR-GOLD.md` · `00_AGENT_CURRENT_LOGIC_[LOAD_AT_START]` |
| Seal phrase | `I AUTHORIZE AGENTS` |
| Sealed at | _(pending operator message)_ |

**Last update:** 2026-08-26 — instrument ready for seal.
