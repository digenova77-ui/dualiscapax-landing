# FINAL CONSTRAINTS

**Status:** Unified 26 Aug 2026  
**Method:** DCLM smash — everything agents can do is done or scaffolded; only irreducible Operator residuals remain.

---

## Already closed (not constraints)

| Area | State |
|------|--------|
| Public residual-law / vision language | Live |
| Research hub visitor copy | Live |
| Unity Framework + closed-loop math docs | Live |
| Blind property IP / autonomous goal canon | Live |
| Grants G1–G7 standing YES | Locked |
| Realtime inbound reply automation | Live |
| Signature / ready-for-handoff **protocols** | Locked |
| Agent release after handoff folder | Locked |
| Fused A×B design (no serial YES) | Locked |

---

## Unified “can’t” → final constraints only

All prior yellow items collapse to **three** Operator acts. Nothing else is blocking system autonomy growth at the information plane.

### FC-1 — MAIL_UNITY_CLOSE (identity organ)

**Why irreducible:** Agents cannot create Workspace/SMTP, edit SPF/DKIM at CF, or finish Gmail Send-as verification UI.

**One sitting:**
1. Workspace *or* SMTP for `dualiscapax.ai`
2. SPF + DKIM for that path
3. Outside → admin@ lands in **connected** agent mailbox
4. Send-as admin@ + ceo@
5. Signal: **`MAIL_UNITY_CLOSE DONE`**

**Closes:** End A + End B + From: Dualis + auto-reply native identity.

---

### FC-2 — DOMAIN_UNITY (web edge)

**Why irreducible:** No Cloudflare API token in agent hands; .com / ratio still Squarespace “Coming Soon.”

**One sitting:**
1. CF (or authoritative DNS) 301: `dualiscapax.com` + www → `https://dualiscapax.ai`
2. Same for `ratio-dualis.com` (+ any other ratio hosts you name)
3. Optional: token later for agents to maintain — not required if you click once

**Closes:** G1 execution. All public names → one lander.

---

### FC-3 — SIGNATURE_ASSET (print packs only)

**Why irreducible:** Wet-ink / markup file must come from Operator once.

**One act:**
1. Upload signature to private Drive (or attach)
2. **`SIGNATURE LOADED`** + allowed classes (e.g. CRA/GOC print packs)
3. Agents stamp only named packs → **`ready for handoff`** → **you** deliver → agents **released**

**Does not block:** mail identity, site, learning loops. Blocks only image-complete government print packs.

---

## Explicitly NOT final constraints

| Item | Why not |
|------|--------|
| Serial YES on mail | Replaced by FC-1 once |
| Grafana | Internal scoreboard; optional |
| BIMI | After FC-1 only |
| Seat open | Business/IP lock — separate product residual when you choose |
| Agent learning / research bind | Ongoing; not waiting on FC |

---

## Order if only one pass

1. **FC-1** (mail = product surface for information return)  
2. **FC-2** (all doors → .ai)  
3. **FC-3** (when a CRA/GOC pack is next)

---

## One line

**Final constraints = FC-1 mail identity · FC-2 domain redirects · FC-3 signature file once — everything else is already Dualis-side or non-blocking.**
