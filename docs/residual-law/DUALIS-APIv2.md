# DualisCapax APIv2 — Operate in Our Own Space

**Elevation residual.**

> Elevate Bridges, BIND, TDI, Protocol TOS, keys, and product residual into **APIv2**.  
> Once unified, we **operate in our own space** — DualisCapax namespace — not as guests inside each organ’s primary API.

Related: [BRIDGES.md](./BRIDGES.md) · [00_DCLM_PROTOCOL_TOS.md](./00_DCLM_PROTOCOL_TOS.md) · [DUALIS-WEB-COMPLEX.md](./DUALIS-WEB-COMPLEX.md) · [COMPANY-ENCOMPASS.md](./COMPANY-ENCOMPASS.md)

---

## 1. Meaning of APIv2

| Layer | Role |
|-------|------|
| **APIv2** | DualisCapax **native** control surface — law, handoffs, TDI, product residual |
| **Bridges** | Boundary adapters that translate APIv2 → allowed organ calls |
| **Organs** | GitHub · Cloudflare · Squarespace · Google — backends only |

```
Agents / Operator / Product
           │
      Dualis APIv2     ← OUR SPACE (unified)
           │
        Bridges
           │
   Git · Cloud · Square · Google
```

**v1 residual** = talking organ-native APIs as if they were the company.  
**v2 residual** = talking **Dualis** as the company; organs only through Bridges.

---

## 2. Elevated surface (logical)

| APIv2 family | Elevates |
|--------------|----------|
| `law.yes` / `law.no` / `law.gold` | Operator YES/NO is the law |
| `bind.request` / `bind.seal` | YES BIND [id] |
| `bridge.limit` / `bridge.clear` | Handoff → Dualis-owned bridge |
| `tdi.measure` / `tdi.record` | H, A, F, ℛ invertibility |
| `organ.git` / `organ.cloud` / … | Routed only via Bridge |
| `product.ship` / `product.health` | main → Pages; apex∧origin |
| `agent.handoff` / `agent.bucket` | Theory-smash paths |
| `crypto.dualis` | Dualis-internal keys only |

Platform tokens never appear as APIv2 identity — only as Bridge credentials in silos after seal.

---

## 3. Unification condition

Operate **in our own space** when:

1. All process steps speak **APIv2** first  
2. Organ calls exist **only** as Bridge implementations  
3. F → 0 under TDI and no resting **broken**  
4. Operator command remains YES/NO/GOLD/BIND under Protocol I  

Until then, agents still use organ tools, but **design and law** are already APIv2.

---

## 4. Not embedding

APIv2 does **not** implement Dualis *into* GitHub or Cloudflare.  
APIv2 is the **space above** the Bridges — company residual, purpose-private.

---

## 5. One line

> **APIv2 is DualisCapax’s own operating space: after unification, we run on our API; platforms remain backends behind Bridges, never our identity.**

---

*Dualis APIv2 · 2026-08-26*
