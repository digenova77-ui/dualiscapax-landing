# Lessons — factory broadcast

Every unit reads this file before work. Append-only. Tune in place when the same class repeats.

## L-001 AMNESIA — two-folder publish
- Bot: `unity:publisher.clerk`
- Watch: `unity:publisher.watch`
- Class: AMNESIA
- Cite: live apex served root `index.html`; `cf-pages/` is warehouse
- Avoid: never treat Wrangler 10000, a zip, or `cf-pages/` as the street
- Tune: curl the apex and diff to root `index.html` before saying live

## L-002 308 — ice door
- Bot: `unity:rte.ice`
- Class: 308
- Cite: `GET https://dualiscapax.ai/ice` self-named 308 / 503 / 403
- Avoid: do not sell a room whose door loops
- Tune: Iris says the door is not open. Fix the rewrite or take the link off the hall

## L-003 HOLE — engine not imported
- Bot: `unity:publisher.clerk`
- Class: HOLE
- Cite: `js/engine-runtime.js` and `js/engine-link.js` exist; root index did not load them
- Avoid: do not claim the engine is on the site if the lander does not import the stepper
- Tune: script tags on root `index.html`, then curl until the src appears

## L-004 NOT_FOUND — jacket v2
- Bot: `unity:jacket.watch`
- Class: NOT_FOUND
- Cite: `/api/iris` 200 BYOK; `/api/v2/chat` 404
- Avoid: do not tell a piper V2 is on the apex until that path 200s
- Tune: keep `/api/iris` as the live pipe; V2 stays a plate until a worker exists

## L-005 PARKED — till fulfill
- Bot: `unity:till.watch`
- Class: HOLE
- Cite: `/hooks` 404; checkout_open false
- Avoid: do not say a paid pack lands a seat
- Tune: Buy Buttons may sit on `/pay`. Fulfill waits on a signed worker

## L-WAV MUTE — greet take missing
- Bot: `unity:wav.watch`
- Class: MUTE
- Cite: `https://dualiscapax.ai/audio/iris-greet.wav` 404; `audio/` on main is README only
- Avoid: do not call TTS a successful greet. TTS is a mute-guard and a hole.
- Tune: drop a ≥50KB speech take on root `audio/`, load `js/iris-wav.js`, curl until 200
