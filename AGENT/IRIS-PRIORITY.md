# Iris is the highest alert

Stamp: 2026-09-23T14:55Z
Seat order: Iris street holes outrank harvest, ice, golf, coin paper, law-file theater.

Class: `IRIS-CRITICAL`
Tickets now: `L-WAV` then ring-on-street then speak-stream.

## Rank

1. Hello plays on `https://dualiscapax.ai/` — `audio/iris-greet.wav` 200 and `js/iris-wav.js` on the live index.
2. Ring stays awake after silence — keep-alive from `js/iris-ring.js` is on that same page.
3. Speak-stream for novel lines — after 1 and 2. Needs a Seat secret. Do not block 1 on it.

## Who sits

| Desk | This hour |
|---|---|
| flow floater | L-WAV only |
| develop.clerk | splice index + audio |
| publisher.clerk | push main, curl apex |
| wav.watch | stay CRITICAL until curl 200 |
| harvest OHF ping / golf | THIN |
| OMHA / OWHA harvest | keep packs, no new general search |

A harvest cron that writes while L-WAV is open is a privilege hole only if it splices the lander. Pack writes may finish their tick. They do not take the floater.

## Fail closed

Do not open a second lander.
Do not write another manifesto instead of the splice.
Do not claim the apex flipped if curl still 404s the take.
