# CURTAIN — Unity ID before depth

Stamp: 2026-09-09T22:40Z
Look is free. Depth asks for a Unity session. Cost is $0. KYC is later.

## Split

- Look (`why.html`, `story.html`, `ca.html`, `world.html`, hall): no ID.
- Depth (`encyclopedia.html`): `js/curtain-gate.js` sends the visitor to `curtain.html`.
- Unity ID: minted on-device in `js/unity-session.js`. UUID. Not a government stamp.
- OAuth (Google / Microsoft / Meta): PAPER. Buttons disabled until Seat puts client IDs in the worker dashboard. Do not paste secrets into git.

## No Ontario default

Spine default is WORLD.
America/Toronto is a hint, not an auto-land on Ontario.
America/Edmonton suggests Alberta.
Asia/Kathmandu suggests Nepal.
The visitor confirms the desk on the curtain.

Operator house (OBCA #100089211, Belleville) never becomes the visitor landing.

## Cloudflare secrets

Chat cannot mint `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, or Stripe `whsec_`.
Seat pastes those into repo Actions secrets and `wrangler secret put`.
This file does not contain them.

## Loop

Declared `jx` is stored on the device with the Unity ID.
The jurisdiction adapter reads `dc_jural`.
The jural watcher still fails if Québec pages paint OHIP.
That is the feedback loop that exists today: declare → paint → watch.
It is not a self-training model. Do not claim it is.
