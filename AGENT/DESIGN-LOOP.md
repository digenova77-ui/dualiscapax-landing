# Design loop = engine loop

AI agencies on the hall use the same autonomy as the RTE.
No second physics for “creative.” Vault locked.

## Same brick

| Engine / Unity RTE | Design agency |
|---|---|
| `webhook_event` append | git commit on `main` |
| `unity_fields` snapshot | `AGENT/NOW.md` |
| `/u/fields` read | `AGENT/PROMPT.md` then probes |
| residual-ring curl | same ring hashes hall + AGENT |
| Foreman `/site/status` | LIVE/PAPER/RED/CLOSED brief |
| Handoff on necessity | next kind from ROSTER |
| CHECKOUT_OPEN | still false — design does not flip it |
| Iris reads speakable fields | agency reads speakable spine |

Session is the breath. Deploy is the skeleton. Do not hot-patch five rooms from a swarm.

## Autonomy they share

- Time only forward. A bad card is a new commit, not an edited yesterday.
- One floor per kind. Affinity = receipt for the next kind.
- Probe beats chat. Git + curl beat NOW.md if they disagree.
- Web RTE and app RTE will read the same Unity stream. Design agencies already read the same spine.

## What is not equal yet

Hall engine is files + ring. Unity RTE (`/u`) is paper. Design loop is this chat + git + PROMPT.md.
They become “as autonomous as each other” when `/u/fields` is live and NOW.md is updated by the ring, not by memory.
Until then, agencies fake nothing. They splice station 5.
