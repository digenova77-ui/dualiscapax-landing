# Never commit

Stamp: 2026-09-23T14:42Z
Open GitHub stays open. Crawlers welcome. These strings never land on main.

sk_live_
whsec_
CLOUDFLARE_API_TOKEN=
CF_API_TOKEN=
XAI_API_KEY=
xai-
BEGIN PRIVATE KEY
BEGIN RSA PRIVATE KEY
BEGIN OPENSSH PRIVATE KEY
ghp_
gho_
ghu_
ghs_
github_pat_
AKIA
aws_secret_access_key
MONGO_URI=
DATABASE_URL=postgres
STRIPE_SECRET
sk_test_

Public on purpose:
- Stripe pk_live_ / buy_btn_ embed on the lander. Publishable. Not a secret.
- Repo names, apex URLs, Unity ID schema, harvest cites.

If a watch finds a never-commit string on main: ticket CRITICAL, rotate the key, rewrite history only if the key was live. Do not "fix" it by committing the key again inside a doc.
