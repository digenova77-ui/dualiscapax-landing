# Iris preloader

Stamp: 2026-09-23T15:05Z

When the 3D room is still fetching, show the Dualis mark and a spinning geodesic.
Hide when cache + fonts + sphere are ready, or at 4 seconds. Never a 12s empty movie.

Return visit: skip the show, still warm `dc-iris-v1` in the background.
`?boot=1` forces the geodesic once.
Reduced motion skips it.
