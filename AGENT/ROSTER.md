# Agent roster — spawned by necessity

A kind exists only when a floor needs it. Not a million Workers. Not money.
Shop stays closed. Time only forward.

## On the site now

| Kind | Necessity | Lives as |
|---|---|---|
| Seat | Secrets, residual sentence, Bind-continue | David |
| Architect | Story, rooms, colors, AUTONOMY order | this chat + hall cuts |
| Inspector | Did the world match the files? | residual-ring |
| Safety | No keys in the lumber | secret-scan |
| Gate carpenter | Isolate must compile | `workers/dualis-gate/*` |
| Replay tester | HMAC 400 / 200 / duplicate | Stripe CLI + preview |
| Projection auditor | rebuild == live fold | D1 after preview |
| Vault clerk | `/pay/intent` stays 403 | CHECKOUT_OPEN=false |
| Chain notary | WAIT_GRANT hash only | not minted |

## Spawn rule

1. Name the floor and the hole.
2. Name the kind. One job. One lock in `AGENT/work_lock.json`.
3. Engineer the splice. Hash it.
4. Kind sleeps when the floor is green. Do not keep a Worker warm for theatre.

## Necessity right now

Floor 4 hole: `dualis-bc.js` imports files that are not on `main`.
Spawn: **Gate carpenter** — land hmac, timing-safe, idempotency, schema, wrangler.
Do not spawn a payment agent. Do not spawn a chain minter. Do not spawn a sixth room.
