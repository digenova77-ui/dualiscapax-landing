# DualisCapax autonomy spine — 2026-09-09

David is the seat. Time only goes forward. Checkout stays closed until Bind-continue.
This file engineers the framework. It does not claim the framework finished itself.

## Law
Look $0. Append events, fold projections, never rewrite the log. Merge to oldest root. Speakable fields only. Public-chain is WAIT_GRANT. Compensate is a new event. If you did not deploy it, it is not live.

## Looping now
hall/ in git. residual-ring watches hall + curls /hall/ (42d6239, run 1792). Pages + secret-scan green. Apex /hall/ still reroutes. workers-live red. Checkout closed.

## Seat order (back-compat)
1 Bind apex /hall/ to this Pages publish
2 Human vo/*.wav — TTS off
3 Preview dualis-gate HMAC five greens — never bind /*
4 /u/session + /u/fields
5 Identity webhook fold
6 Merge ledger
7 PWA + invited runtime same stream
8 UnityFan DO sockets
9 Outbox + queue drain — no refund
10 Residual sentence + Bind-continue then CHECKOUT_OPEN
11 Chain receipt still WAIT_GRANT

Do not skip to 11 because 1 is still rerouting. GitHub greens are not Iris memory. Truth prevails. Leftover first.
