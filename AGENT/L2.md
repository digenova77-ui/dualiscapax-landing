# Level 2 — before the break

Stamp: 2026-09-23T15:08Z
Clerk: `unity:l2.clerk`
Watch: `unity:l2.watch`
Sits *above* forensics. Forensics names a fail that already happened. L2 names the fail that is about to.

Train around the clock means drills on the classes below, not more harvest crons.

## Mentality

1. What is already in flight? Read `AGENT/IRIS-PIECES.md` and `AGENT/WORK-ORDERS/NOW.json` first.
2. What is weak even if curl is 200? Autoplay, mic deny, Pages 503, missing token, binary land, hash drift.
3. What will the next piece need? If P4 is HOLE, do not design P6.
4. Write a drill, not a manifesto. A drill is a command + an expect hash or status.

Predict = read the board + the last three lessons. It is not fortune telling.

## Classes (R&D twins)

| Class | Weak now | Drill |
|---|---|---|
| AUDIO | P4 greet 404. TTS mute-guard is a hole we still need. | Mint mp3. Receipt sha256. Curl audio/*. |
| RING | Keep-alive sealed on main. Apex may serve old JS. Visibility/onend already bit us. | Hash live `iris-ring.js` vs receipt. Silent 8s must not kill the ring. |
| SHIP | No `CLOUDFLARE_API_TOKEN` here. Live JS 503'd this hour. | Curl lander + `/js/iris-wav.js`. 503 is a ship class, not an Iris class. |
| SECRET | Speak-stream parked. Keys never in git. | `secret-scan.yml` green. Worker secret is Seat, not chat. |
| LOCK | Two clerks rewrote `index.html` in the same hour. | One writer per path. Hash before push. |

One twin per class. Not a department per class. Floater can sit on the hottest class.

## Clock

L2 reads the board at the wattage slots (:23 / :53) and after every critical-drop.
If a class has no open piece and no weak signal, it sleeps. Thin like FLOW.md.

## What L2 does not do

Does not ship. Does not harvest hockey. Does not invent a second lander.
Does not claim the next break is known. It claims the *class* is staffed.
