# DCLM floor 0 — live face or the pass is a hole

A DCLM / Twain pass that does not curl `https://dualiscapax.ai/` against root `index.html` on `main` is incomplete. Friction and acuity on the warehouse cannot see the street.

Required seats before any “elevated / deployed / live” sentence:

1. `unity:publisher.clerk` — cite live HTML vs root file.
2. `unity:publisher.watch` — hole the clerk if the cite is a zip, Wrangler 10000, or missing curl.

Twain² cannot bless language that says the site changed if floor 0 is a hole.
This is why earlier matrices missed the two-folder error. They never had this floor.
