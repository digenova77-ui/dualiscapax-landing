# DCLM-WEB — website tweak

Stamp: 2026-09-23T13:45Z
Unit: `unity:dclm.web`
Watched by: `unity:publisher.watch`
Does not replace DCLM-FLOOR-0. It is the street version of the matrix.

A website DCLM pass that skips a floor is a hole.
Twain² after the six floors, not instead of them.

## Floors (100 friction each)

0. **Street vs file.** Curl `https://dualiscapax.ai/`. Diff to repo-root `index.html` on `main`. No match → hole. Zip / Wrangler 10000 is not a cite.
1. **Named doors.** Every link or button the live page names must return 200, or the page must say that door is closed. Ice 503 while the hall sells Ice → hole.
2. **Iris.** Orb is on the page the visitor is on. Voice default-on unless they mute. She knows the page. No third-party screen reader pitch.
3. **Till.** Copy on `/pay` and any Buy button must match Stripe state. Deactivated link + “Live checkout” → hole. Seals are not for sale.
4. **Talk.** Coffee-shop words. No factory / wrangler / DCLM jargon on the street.
5. **One face.** Root lander and `cf-pages/` cannot both claim to be the site. Warehouse stays warehouse.

## Twain²

After 0–5: one sentence a visitor would say after ten seconds.
Then the same sentence again, shorter.
If the two sentences fight, the floors are not done.

## Dual pipe

`unity:dclm.web` runs the floors.
`unity:publisher.clerk` fixes the root file.
`unity:publisher.watch` holes anyone who skips floor 0.
