# AUDIT_ALWAYS_v1

Question: doable?
Answer: **yes**, at the GitHub floor. Not sub-second. Not a Pages deploy.

David does **not** invoke the DCLM matrix. The matrix runs.
Iris is **always** the observer. Mouth stays off the plate.

## What “real time” is here

GitHub Actions will not schedule faster than **5 minutes**.
`factory_audit.yml` sits on `*/5` (Clock A). It does not push `main`.
Pages 500/mo stays Clock B.

Faster than 5 min would be a Worker cron Dualis does not put on the apex.

## Bind

```
Idea Engine mint     factory_mill.yml
Live HEAD            factory_verify.yml + factory_audit.yml
32-bit website field factory_audit.yml  (seats Dualis can measure without a human)
Iris                 observer on every card; writes notes only if a later law allows a commit
Human                never required to type “run DCLM”
```

Seats the audit can fill without you: 1 (Home 200), 4 (308-self), 13 (live path), 21 (404 free), 27 (cite or hole on the scoreboard).
Seats that stay human holes until you flip Cloudflare: 18 Worker=None, token Edit, watch-path.

## Iris always

Academy law unchanged: she watches every tick. She does not speak on dualiscapax.ai until `iris=academy_mouth_off_plate` is hashed off.
Every audit card carries `observer: iris-academy`.

Look $0. Home unsmashed. THEOREM_U1.
