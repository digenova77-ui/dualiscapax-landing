# GATES_SECURE_v1

Define every Dualis gate. Secure the bind. Price without SKUs on glass.
Open only AND=1 seats on the next Clock B swallow.
Does not smash index.html. checkout=false. Look $0.

Parents: TONIGHT_LAW_v1, ESCROW_MAP_v1, DCLM_TONIGHT_COLLAPSE_v1, FACTORY_PIPELINE_v1.

## Gates (each is 0|1, AND to open)

| Gate | Bind | Secure how | Open on plate? |
|---|---|---|---|
| Person door | OAuth code + PKCE S256 | no implicit, no password on Dualis | not until row |
| Till door | HMAC-SHA256 of `t.body` | timing-safe compare, Stripe sig | checkout=false |
| Deal fuse | eFuse ESCROW→PURE→SPENT | Residual draft; pay() CEI hole | no |
| D1 till | write_once event_id | ON CONFLICT DO NOTHING | no Fuel fallthrough |
| Look | empty until row | no SKU, no keepBps, no CAD $49 | YES /look /gates |
| Apex | 200 do not smash | additive _redirects only | already 1 |
| Ice/sheet/rink | 200 to FILE | never 308-self | open if HEAD=200 |
| Aide/SIMA | 200 to sima index | same | open if HEAD=200 |
| A la carte | 200 to alacarte.html | same | open if HEAD=200 |
| Iris mouth | academy off plate | law vs lander copy = friction | do not smash Home to fix |

XOR/majority do not open a gate. AND only.

## Price (proper)

- Look / measure / 90-day sim speech: **$0**. Not a product.
- keepBps 81→100: split of a **named save after open**. Not a lander SKU.
- CAD $49 monograph / $380k modeled / $175k kitchen: lander copy vs `look=empty_until_row`. Named friction. Next roll does **not** add more $
- If net verified save ≤ 0: owe 0. That is law, not a price tag.
- Atomic: event_id once. No double grant.

## Next Clock B open list (additive)

1. `/gates` → `gates.html` 200 (this commit)
2. Keep `/look` `/aide` `/alacarte` `/sheet` `/rink` `/why` as already mapped
3. Do not replace `index.html`
4. Do not deploy DualisResidual
5. Do not print keepBps on gates.html
6. Human/CF swallow when Git Connect or Pages upload runs (≥90 min from last Pages touch)

## Collapse

Gates defined = 1. Gates all green = 0.
Open Look. Do not open till.
