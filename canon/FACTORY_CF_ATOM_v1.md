# FACTORY_CF_ATOM_v1

GitHub is the factory. Cloudflare is the nozzle. Dualis does not debug on the live glass.

## Order

1. All precision work happens on `digenova77-ui/dualiscapax-landing` `main` (and cited factory files). Measure, hash, review in git.
2. Cloudflare receives only an **atomic increment**: new file(s) and/or appended `_redirects` lines. The previous LIVE tree is a subset of the next tree.
3. Never omit live `index.html`. Never upload `cf-pages/index.html` over the slot lander. Those are two bases.
4. Live base = **repo root** `index.html` (slots 00–02, hash footer). `cf-pages/` is kennel / other lander — not a smash pack.
5. One CF touch per increment. If the atom is not 200 after that touch, write a hole. Do not spray a second deploy to “see.”
6. Pages drag-and-drop 1,000-file cap is not the factory. Factory uses git. CF Git Connect or one Wrangler folder that **includes** the current lander.
7. D1 / HMAC / eFuse stay write-once. No KV `+=`. No checkout flip from a factory mood.
8. Iris, Merkle, OAuth packets mature in git. Mouth and bind stay off the plate until the increment cites them.

## Atom checklist (before CF)

- [ ] `index.html` bytes unchanged unless a smash grant exists
- [ ] diff is add-only paths + redirect lines
- [ ] file count stays under Pages plan cap (20k Free / 100k paid)
- [ ] no `_redirects` source = dest
- [ ] hash named (TONIGHT_LAW or this file)
- [ ] one measure URL expected 200

## After CF

Measure `/` first. If Home moved, revert. Then the one new path.
