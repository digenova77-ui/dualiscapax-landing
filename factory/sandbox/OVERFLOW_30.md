# OVERFLOW_30

Gates + security = mill (UR-1, PoL-1, overlay AND). Do not re-litigate those here.
These 30 are overflow + architecture David has not closed. Knock them in FACTORY_TEN order.
Look $0. Dualis ≠ coin. No PII. No Home smash. No ice.html full-replace.

Live snap 2026-09-16 19:49Z: Home 200, /rte/easthill/ 200, /ice 308	o /ice (cycle). Not a 404.

## Plate / routing (1–8)

1. **Worker vs Pages map** — which host answers `/ice`. 308-to-self is a loop, not missing file. Cite the Worker route.
2. **Cursed-stem registry** — official unused aliases (`/rink`, `/barn`) so humans have a 200 door while `/ice` stays named-hole.
3. **Single-path linter** — fail CI if both `ice.html` and `ice/index.html` exist.
4. **Two-tree linter** — warn when a plate file is only under `cf-pages/` and live tree lacks it (or the reverse).
5. **`/health.json`** — public curl codes for the current ten. No PII. Measure is the plate.
6. **Preview vs production pin** — one sentence: preview may 200 while apex 308. Don't celebrate the wrong host.
7. **Dead-letter swallow** — ticket stays open until apex curl matches git SHA of that file.
8. **Asset / file-cap meter** — count files on the plate each tick. Stay under Pages 1000.

## Overlay / mill (9–16)

9. **Ticket lease** — first cite owns the path this epoch. Second executor waits.
10. **Epoch id** — date+root so replay is defined without a chain.
11. **Receipt schema v1** — path, old, new, AND, curl, no names.
12. **Overlay CI** — path allow + Home/ice forbidden + curl watch. No CF token required.
13. **Incident log** — one line when a diff almost touched Home.
14. **Promote JPEG gate** — tooling reminder, not a new page: human still eyes it.
15. **Agent Card hole** — `/.well-known/agent-card.json` named, not shipped until AND=1.
16. **Kennel vs plate clocks** — mill cron must curl easthill/ice, not only mint slots.

## Human desks (17–24)

17. **Vault export** — encrypted blob she saves; Dualis never sees phrase.
18. **Multi-device** — public Unity id via QR; phrase typed on the new phone. No server sync of cards.
19. **Offline desk** — service worker for `/rte/easthill/` on school wifi. Cards stay local.
20. **Print CSS** — report draft on paper. Still her words.
21. **en-CA locale ring** — language ≠ tax `jx`.
22. **Board theme tokens** — CSS variables only. No student data.
23. **ICS week** — bells + her plan titles, no child names in the file.
24. **A11y tap targets** — laptop deep / phone simple already law; measure contrast.

## Overflow they have not named (25–30)

25. **Barn ingest hole** — schedule as cite-or-hole. Not Spordle bind. Not invented rosters.
26. **Fuel visible without amounts** — Look shows depth-on/off, not his bank.
27. **CSP for desks** — default-src self + fonts. Capture page stays gated.
28. **Confirm TTL** — idle lock already in vault; apply same TTL on teacher confirm bit.
29. **Grant expiry** — parent/EA bits die at a date she sets. Default off remains.
30. **Want-dead ticket** — explicit empty-receipt type so mill stops a path without inventing 25.

Knock 1–2 before 25–30. Ice cycle is the residual. Easthill 200 is not an excuse to full-replace Ice.
