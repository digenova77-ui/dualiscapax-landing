# HANDOFF CHECKLIST

Source: FACTORY-ELEVATION-HANDOFF-20260916-V2
Scorer: Benjamin 2026-09-16T23:00Z + Grok plate notes.
Git commit ≠ production. L1≠L2 = HOLE.

## 0 Operating directive — DONE as law

Do not rebuild snapshot. Do not replace index.html. Not more pages.
Capability per atom. Hockey is not the architecture.

## 1 Architectural law — DONE kennel

`INSTRUMENT_LOOP.md` + `FACTORY_TEN.md` + `THE_ARROW.md`.
Not a live instrument on Home.

## 2 Latest measurement — HOLE preserved

| Measurer | `/rte/easthill/` | `/ice` |
|---|---|---|
| Lucas (handoff) | 404 | Ice title; 308 client-dependent |
| Benjamin browse this hour | Grade 2 desk title | Ice title |

Disagreement = **not production-green**. Ice stays HOLE. Easthill stays **git×plate**, not GREEN.
Next atom remains two-tree / swallow, not 13 pages.

## 3 Elevation threshold — PARTIAL

Chain exists as files: GREENLIGHT → GENOME → ELEVATION_MAP → TEN.
Factory does **not** yet auto-discover the next page without a human.
Missing: mill cron that reads GREENLIGHT holes and emits one ticket.
`QUEUE.json` is the kennel stand-in.

## 4 Thirteen targets — KENNEL only (correct)

Do not auto-build all thirteen. GREENLIGHT lists them KENNEL.
Next selected by measurement: swallow / two-tree / 308 host map.

## 5 Module genome — DONE kennel (`MODULE_GENOME.md`)

Plate modules do not yet answer the genome questions in the UI.

## 6 Three-measurement model — SEEDED

A = Dualis self-model (factory book).
B = plate curls (disagree this hour).
C = independent DCLM (this checklist).
ΔBC on easthill is the information. Not suppressed.

## 7 Second sandbox law — NAMED, not running as a second host

Kennel yes. Independent measurer = two agents curling. No second Pages project.
`awaiting_second_sandbox_host`.

## 8 Production law — DONE as law

Additive. No index smash. No V280. 308/404/git-only ≠ green.
Functions `_middleware.js` seated; swallow unproven until curl-without-Location.

## 9 Cloudflare (file cut off at CLO) — HOLE

Zone Worker on `*` still `awaiting_dashboard` if 308 persists after swallow.

## What we will not check off tonight

Thirteen instrument URLs. Ice cockpit. Easthill PRODUCTION-green. Coin on Look.
