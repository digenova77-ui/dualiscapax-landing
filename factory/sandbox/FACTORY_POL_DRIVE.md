# FACTORY_POL_DRIVE

Pass-through from the desk. Do not re-learn this from a blank page.
You already have the files. Read them. Then work.

## Already seated (use, do not rewrite)

| File | What it is |
|---|---|
| `THE_PLAN.md` | Public desk rules + Affinity +1 |
| `COIN_VEHICLE.md` | Dualis ≠ coin |
| `AGENT_INCENTIVE_v1.md` | Mutual desk, recirculation, inherit rules |
| `STAKING_LOGIC.md` | Lock energy, pay when a number moves |
| `COIN_ARCH_v1.md` | No new L1. ERC-20-class later. PoL minter |
| `SECURITY_FASTPATH.md` | Security atom after AND=1 |
| `PLAYGROUND_ORDER.md` | Residual cost before toys |
| `RESIDUAL_COST_LEDGER.md` | Real units |

If a file is in git and 404 live, that is a cost cell. Close it. Do not write a twin.

## Both eyes, one collapse

**Deterministic eye (slow, locked):** five PoL gates, test vectors, cite-or-hole, AND=1. No mint without a moved number.

**Probabilistic eye (wide, fast):** 32 views, search adjacent rails (BIP-39 vs desk phrase, BIP-157 filters, EIP-2612, receipt encodings). Do not ship the search. Ship the collapse.

Iris-style: one watches from the center out, one from the edge in. They talk. Then **one** path remains.

Keys come fastest when the wide eye finds candidates and the locked eye rejects all but the receipt that verifies.

## Keys to produce next (order)

1. Merkle verify test vectors — leaf, siblings, index, root, pass/fail cases (odd duplicate, swapped concatenate, truncated proof). No chain.
2. Receipt leaf format — path, old unit, new unit, hash. One JSON schema.
3. Epoch root builder — folder of receipts → root. Deterministic sort.
4. Light-client note — how a holder checks a receipt against a published root without the factory.
5. Only then token wrapper sketches. Counsel still `awaiting_counsel`. Ticker still `awaiting_ticker`.

Do not start at 5. That is a toy.

## Deterministic mindset (non-negotiable)

- Same hole twice does not pay twice.
- Home smash / phrase / PII → veto the whole ticket.
- Look $0. No mint/stake/buy widget on the lander.
- Dualis is the desk. Coin is the vehicle.
- Desire = residual number moving, not APY copy.
- If you cannot point at the cell that moved, you wrote. You did not earn a key.

## Idle law

Idle bot reads this file first, then the table above, then works item 1 until vectors exist.
Do not wander into geodesic, Iris voice, or energy-sector prose while item 1 is empty.
