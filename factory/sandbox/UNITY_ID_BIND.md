# Unity ID bind — factory law

**Spine:** one device phrase → one Unity ID → many role doors.

```
Look (L1)          no vault required
Portal phrase      DCVault.create / unlock
Unity ID           public handle on this device only
  ├ Ice            role=ice     + roster proof
  ├ Teacher        role=teacher + board look + confirm
  ├ SIMA           role=sima    + architecture desk
  └ visitor        default
```

## What bots may write on the plate (after AND gate)

- `cf-pages/js/unity-bind.js` — joint: read DCVault.session(), hatch Unity ID, attach role. No network.
- `cf-pages/rte/unity/index.html` — live seat: phrase state, roles present, doors. Look $0 copy.
- Additive `_redirects` line: `/unity-id  /rte/unity/index.html  200`
- Teacher bind/desk: require `DCVault.session()` before class tools. Same eight words as Ice.

## What bots must not write

- A second word list
- Student or child names
- `cf-pages/index.html`
- Full replace of `ice.html` or `unity.html`
- Fake OIDC / TeamSnap tokens
- Helix, extra Worker named landing

## plate_live watch (after swallow)

Add curls:

- `https://dualiscapax.ai/portal`
- `https://dualiscapax.ai/unity`
- `https://dualiscapax.ai/rte/unity/`
- `https://dualiscapax.ai/rte/boards/phrase.html` (if seated)

403/308 = hole. Do not invent 200.

## Perpetual mill slot

`Q31` Unity ID joint: hatch teacher + ice + sima off one vault.
When idle, deepen receipts. When a human says JPEG matches, copy **one** file.
