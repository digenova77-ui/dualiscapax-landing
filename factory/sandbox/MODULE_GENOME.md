# MODULE_GENOME

Every module carries:

id, path, tree (plate|kennel), state (IDEA→KENNEL→L1→L2→GREEN-LIT→CANDIDATE→PRODUCTION|HOLE|AWAITING),
loop_beats_it_owns, depends_on, authority_needed, measure (how to curl),
falsify (what would prove it failed), last_observe (code + when).

L1 vs L2 disagree = HOLE.
No invented API. No silent privilege.
Hockey is an instance of the genome, not the genome.
