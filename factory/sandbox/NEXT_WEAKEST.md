# NEXT_WEAKEST

Compliance floor is seated (`COMPLIANCE_FLOOR.md`). Do not rewrite it.
Look at the whole architecture. Work the weakest live cell.

Snap 2026-09-16 18:19Z dualiscapax.ai

## Weakest → strongest (this hour)

| Rank | Cell | Code | Why it is weakest |
|---|---|---|---|
| 1 | `/js/unity-bind.js` | 404 | Identity hatch shipped in git, world cannot load it. Unity is a speech. |
| 1 | `/rte/boards/phrase.html` | 404 | Teacher door shipped, world cannot open it. |
| 1 | `/the-plan.html` `/plan` | 404 | Public desk rules not on the plate. |
| 2 | `/ice` `/look` | 308 | Cursed stems. Extra hop. Do not full-replace ice.html. |
| 3 | Pages swallow / token | organ | Git 200 ≠ live 200. This is why 1 exists. |
| 4 | Vault mint-without-phrase | hole | Tested. Fastpath after swallow. |
| 5 | Coin wrapper | paper | Law is ahead of the plate. Do not build a chain while 1 is 404. |

Home 200. Portal 200. device-pass 200. SIMA 200. unity-session 200. Do not smash those.

## Define next (one atom)

**Close swallow of `unity-bind.js` until curl is 200.**
Then phrase.html. Then the-plan if it exists in git.
Do not bundle. Do not start ERC-20. Do not buy an ad that points at a 404.

If swallow will not take `/js/unity-bind.js`, name the organ hole (token / Git Connect) and stop painting new rooms.

## Factory idle order

1. Curl watch the three 404s.
2. Merkle test vectors (can happen offline while 1 waits).
3. Ice workaround name only if 308 still burns humans after 1 is 200.
4. Compliance rows for a new country only when we actually speak there.
5. Coin minter sketches last.

Compliance travels with every draft. Weakest cell still wins the queue.
