# Unity Playground — sandbox vision

**Not live.** Live mouth is `cf-pages/` → dualiscapax.ai.

Ultimate vision the idle factory builds toward:

- One house. Home (lander) stays the door.
- Rooms a human can tap: Look, Ice, SIMA desk, À la carte, Playground.
- Playground is where Unity / DCLM / Iris *rehearse* before they touch the door.
- Cite-or-hole in every room. Hole stays a hole.
- Hockey path (OMHA → OHF) only from public cites.
- EHR aide only from the job we modeled — no PHI.
- No advertising until private-tab 99.99% (Home + Ice + linked rooms 200).

Idle tick writes a receipt in `factory/sandbox/receipts/`.
It may refine `playground.html` **in this folder only**.
