# ELEVATION_MAP (A–J)
Observe 2026-09-16T22:24Z. Do not treat this file as production.

## A. What exists now (apex)

PRODUCTION 200: `/` `/portal` `/unity` `/rte/easthill/` `/js/device-pass.js` `/js/unity-bind.js`
HOLE 308: `/ice` `/look` `/hockey` (cycle class). Easthill siblings class/share also 308 after index 200 — two-tree / worker.
KENNEL: UR-1, PoL-1, overlay, OVERFLOW_30, instrument loop, this map, 13 elevation specs (not URLs).

## B. What each demonstrates

Home = mouth exists. Portal = phrase door. Unity = seat copy. Easthill = Grade 2 door. JS = vault+hatch code. Ice/look/hockey = friction object (308-to-self), not missing HTML in the book.

## C. What each fails

None of the 200s run the full instrument loop on-page (question→evidence→delta labeled). Ice cannot demonstrate a cockpit while OBSERVE is a cycle. Hockey page cannot be the proving ground until the stem resolves. Green-light board was not machine-readable before this tick (now kennel JSON).

## D. Highest-value missing

1. Host map for 308 stems (Worker vs Pages).
2. Machine-readable board on a 200 path (`/js/modules.json` once measured).
3. Friction object for `/ice` as data, not prose.
4. Evidence labels on Easthill (observed vs local modeled cards).
5. Question engine fed by GREENLIGHT.json holes — kennel script, not a lander.

## E. Dependencies

Host map → Ice/hockey observable → hockey-as-pattern demo.
GREENLIGHT.json → question engine → next ticket.
Vault JS 200 → teacher confirm → grants sent=false.
PoL/UR-1 do not depend on Ice 200.

## F. Kennel without touching production

All 13 elevation targets as files in factory/sandbox/.
Question engine = read GREENLIGHT holes, emit one ticket.
Perspective farm = 32-eye table already in UR-1 / Unity matrix.
Module genome + this map.

## G. Need external authority

TeamSnap/OHF live registry. Board OIDC. CRA/broker. CF Worker edit for /ice. Counsel for coin. Capture of a classroom.

## H. Blocked holes

`/ice` `/look` `/hockey` 308. Invented rosters. Iris on kid AV. Coin on Home. sent=true grants. World tax engine.

## I. Next production atom (shortest)

Not a new lander.
Either: seat `js/modules.json` (green-light snapshot) on the live js/ tree that already 200s,
OR: unused-stem 200 for hockey/ice if /rink already serves.
Do not replace index.html. Do not full-replace ice.html.

## J. How the factory learns

Re-curl the ten. If GREENLIGHT.http disagrees with curl, state=HOLE even if git is green.
L1 (book) vs L2 (apex) mismatch = HOLE.
Next question = first HOLE in the JSON, not a new essay.
