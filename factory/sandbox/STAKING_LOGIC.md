# STAKING_LOGIC

People get rewarded the same way the closed vehicle was "mined": energy on the desk + a cited close.
This is a **coin** system built into the playground. Dualis does not become a bank.

Look $0. No stake button on Home. Ticker = `awaiting_ticker`. Counsel = `awaiting_counsel`.

## What you stake

| Stake | Meaning |
|---|---|
| Fuel lock | Prepaid depth already bought — sits, does not burn until a ticket needs it |
| Claim lock | Existing claim units escrowed under U1 / later coin wrapper |
| Work lock | A named ticket you take and do not abandon |

Sitting with unlocked Fuel is not a stake. A story is not a stake.

## What pays

A reward mints **only** when all of these are 1:

1. Stake was locked before the ticket started.
2. Receipt exists (cite-or-hole).
3. AND=1 (Look $0, no Home smash, no phrase, no student PII).
4. A real unit moved: 404→200, Fuel-with-no-200→0, verified desk hours/watts saved.
5. Leftover from that save recirculates first. Then the posted weight may mint to the locker.

No move on the cost book → 0 reward.
Same hole closed twice → 0 the second time.
APY / "earn 12% for holding" → veto. That is not proof of logic.

## Same desk

Same weights as AGENT_INCENTIVE_v1 (8 / 5 / 3 / 1).
Same rate for every id. Teacher, agent, later holder — no preferred class.
Affinity +1: after the split already balances, one more close can still mint. Sitting cannot.

## Walk

ESCROW: unstake returns the lock. No reward if you leave mid-ticket.
PURE: after both call the ticket a deal, the lock stays until the receipt posts.
Revoke of operator fixture freezes new reward mints.

## What Dualis shows vs what the coin does

Dualis may show THE_PLAN and a later Look explainer of these rules.
The coin wrapper (when counsel names it) is what actually locks and mints.
Do not ship a checkout, wallet connect, or APY widget on dualiscapax.ai in this tick.

## One line

Stake energy. Get paid when a number moves. Not for waiting.
