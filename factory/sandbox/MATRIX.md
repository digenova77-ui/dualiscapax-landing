# DCLM MATRIX — screens × depth (operator, not architect)

If I am the human who has to tap this at 6am, I do not want 22 siblings.
I want a desk, one job, then the cite or the hole.

## Screens I will actually use (plate)

| # | Screen | Why it exists |
|---|---|---|
| 0 | Home `/` | The mouth. 200 or nothing else matters. |
| 1 | RTE desk `/rte` | Orbs then five job taps. Waiting on Pages. |
| 2 | SIMA floor `/rte/sima-dclm/` | Already 200. The room behind the desk. |
| 3 | Connect | integrate.html — two systems. |
| 4 | Upgrade | upgrade.html — already in. |
| 5 | Stand up | implement.html — new thing. |
| 6 | Pair | pair.html — business contacts. |
| 7 | The job | job.html — as she named it. |
| 8 | Ice `/ice` | Family hockey. Still 308 hole. |
| 9 | Look `/look` | Look-only. |

That is **10 screens**. That is the matrix width.

## Depth per screen (hops, not more pages)

Hop 0  What am I doing. One sentence.
Hop 1  The tap that is that job.
Hop 2  Official cite or `awaiting_*`.
Hop 3  Stop. No fourth page that repeats hop 0 in bigger words.

Depth = 3. Amplitude is how hard hop 2 is checked. Not a 15th HTML file.

## Files that exist but are not extra screens

SIMA folder also has: after, books, cutover, dclm, deal, door, framework, iris, join, law, leftover, tap, theatre, walk, weekend.

Those are **drawers**, not a second desk. Promote one only when a tap on screens 3–7 needs it. Leftover stays leftover until a cite.

## What I refuse

- 22 tabs in the footer
- Ice + ice/ + ice.html
- A graph visualization instead of a tap
- Kid bios on RTE
- Contracts tap with no signed file

## Score

Width 10. Depth 3. Affinity +1 = curl the screen you just named.
If it is not 200, the matrix is a drawing.
