# NEXT_PUSH — off-nozzle work

No new public pages this file. Refine ideas. Execute on the next L3 atom.

## Allowed on the live site (not a new room)

- HEAD / GET existing 200 rooms: `/` `/hockey` `/sandbox` `/rte/sima-dclm/` `/portal` `/how` `/care`
- Click what is already there. Speak Iris only if the lander already offers it.
- Move SIMA sliders (client-side). Do not invent a chart ID.
- Read `CF-Cache-Status`, `Location`, title. Mark 308-self as cursed, 404 as free.
- Read git HTML and `_headers`. Rewrite copy in kennel first.
- Stripe / D1 / HMAC checks in Workers kennel. No new route on apex.
- Factory receipts under `factory/sandbox/` with `[skip ci]`.

## Veto

- New public HTML room
- Replace `index.html`
- `wrangler pages deploy cf-pages`
- 308 alias onto a cursed stem
- Cache Everything on HTML
- Worker Custom Domain on `dualiscapax.ai`

## Ideas ready for the next atom (from live 2026-09-16)

1. Home copy: “Look and measure is 100% free” → one plain sentence. Keep Look $0. No new tab.
2. SIMA: keep `/rte/sima-dclm/` (200). Do not rename. Tighten “aide not hire” + closed till. Look $0 stays visible.
3. Ice: do not fight `/ice`. Next unused 404 stem, 200-rewrite to `ice.html` / `rink.html`.
4. Footer links that point at `/ice` `/look` `/alacarte` — retarget to working 200 rooms in the same files.
5. Iris: academy only until mouth is proven in kennel audio. No plate mouth.
