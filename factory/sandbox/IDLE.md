# NEVER IDLE — factory sandbox queue

When a unit has no bigger cite (no harvest, no Ice hole, no SIMA cite),
it works here. It does **not** touch `cf-pages/`.

## Order of work

1. Ice live 200 (plate) — bigger than this queue.
2. SIMA paths that already exist — bigger than this queue.
3. Else: elevate `factory/sandbox/` Unity Playground.

## Forbidden

- Writing `cf-pages/index.html`
- Adding `ice/` next to `ice.html`
- Inventing players, savings, or famous-person replies
- Paid ads / @elonmusk blasts
- Helix, extra Worker named landing

## Promote rule

Sandbox becomes plate only when a human says the JPEG matches
and private-tab 200s. Merge copies one file. Swarm does not.
