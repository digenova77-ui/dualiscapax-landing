# INSTRUMENT LOOP

A web module is not a brochure. It is an instrument if it can run this loop and show the receipts.

WORLD → QUESTION → REQUIRED EVIDENCE → OBSERVE → MODEL → UNKNOWN
→ DELTA → FRICTION → FRONTIER → CAPABILITY → AUTHORITY
→ NEXT MOVE → OBSERVE RESULT → EXPECTED vs ACTUAL → ΔSTATE → MODEL UPDATE → NEW FRONTIER

## Dualis map (already seated)

| Loop beat | Dualis organ |
|---|---|
| WORLD | named want + ring + plate URL |
| QUESTION | Factory_TEN cue |
| REQUIRED EVIDENCE | cite-or-hole |
| OBSERVE | live curl, not git |
| MODEL | collapse 32 → 1 |
| UNKNOWN | awaiting_* |
| DELTA | old code vs new vs live code |
| FRICTION | 308, 404, two-tree, token |
| FRONTIER | what we cannot know yet |
| CAPABILITY | git write, curl, kennel |
| AUTHORITY | confirm, board policy, CF Edit |
| NEXT MOVE | one atom on the live tree |
| RESULT | re-curl |
| EXPECTED vs ACTUAL | 200 vs 308 |
| ΔSTATE | plate changed or not |
| MODEL UPDATE | receipt JSON |
| NEW FRONTIER | still-open list |

## Promotion

IDEA → KENNEL → L1/L2 → GREEN-LIT → PRODUCTION only if apex curl matches the claim.
Git commit ≠ production.

## Live facts (this hour)

- `/rte/easthill/` 200 = loop closed once.
- `/ice` 308-to-self = instrument failed; do not full-replace ice.html.
- Home 200 = do not smash.

Hockey may prove the loop. Hockey is not the loop.
