# FACTORY (unified)

Purpose: close leftover on the live plate so a human or agent can do the next real job.
We are the arrow. Input → output. Git commit ≠ production.

## Load (every unit, including Iris)

1. This file
2. `sandbox/PRODUCTION_LINE.md`
3. `sandbox/QUEUE_FROM_MEASURE.json` — ticket 1 or empty
4. `sandbox/CURL_VERIFY.md`
5. `sandbox/UR1_STANDARD.md` if the ticket is a seat
6. `sandbox/DCLM_ON_SELF.md` if we are about to write a new law

Do not open a new standard first.

## Purpose map (three books)

| Book | Serves | Must not |
|---|---|---|
| Dualis desk | Company plate: Home, Ice desk, SIMA, teacher door | Coin ticker, CRA file |
| Human seat | Phrase + confirm on device | PII in git |
| Coin | PoL-1 after counsel | Pay sitting, Look $0 stays |

## Production atoms allowed

Additive file on the **served** tree + measure 200 no Location.
Working hockey mouth: `/ice-desk/`.
`/ice` is a named hole until HEAD agrees.
Home `/` is not a rewrite target.

## Efficiency

- One ticket.
- Kennel markdown does not need a Pages deploy (`[skip ci]`).
- Verify cron does not push `main`.
- Watch may write one receipt.
- 28 hobby workflows do not each get a lander.
- `pages_build_output_dir=.` is a known mass hole — do not flip tonight.

## Power

Power is a curl that can fail. Power is two measurers. Power is not 13 engines on day one.
