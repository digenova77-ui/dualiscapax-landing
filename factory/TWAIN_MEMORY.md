# Twain memory

Street = `cf-pages/` zip via `.github/workflows/pages-direct-upload.yml` to Cloudflare project `dualiscapax-landing`.
Repo-root edits are kennel until that zip contains them.

Never rewrite `/hockey` to `hockey.html` after that file is gone. That 404 was ours.

Working class: `folder/index.html` (same as `/rte/ice/`).
Two trees: repo root and `cf-pages/`. Claim live only after the zip rail has the file.
Home is `cf-pages/index.html`. Do not smash it to ship a desk.

Live check 2026-09-23: `/hockey` and `/hockey/` 200 no Location.
